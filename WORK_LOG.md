# Work Log

## 2026-08-18

### 요청: "파일 업데이트했는데 확인 좀 해줄래?"

사용자가 `26년 8월 건기식 Aging 리포트 260813 v1.4.xlsx`를 다시 작성해 리포에 올렸다.
실제 파서(`npm run deploy -- --dry-run` + Node 재현 하네스)로 검증하고, 이어서 배포까지 했다.

### 엑셀 쪽에서 바뀐 것

1. **`기초재고` 시트가 8/1 기준 월초 스냅샷으로 정비됨** (`기초재고!AM2 = 8/1/26`).
   기존 `현재고` 시트는 조회 시점(8/13) 그대로 남아 있다.
2. **`현황` 시트 8월 블록의 SUMIFS가 `현재고!` → `기초재고!`로 옮겨졌다.**
3. 직전 파일에서 지적했던 `(채널)유통채널` 수식 미채움·VLOOKUP `#N/A`가 전부 채워졌다.

### 그래서 코드가 안 따라가면 생기는 문제

`현황` 시트만 기초재고로 옮겨졌으므로, 원장을 계속 `현재고`로 읽으면
**①탭 KPI 카드(현황 기준 78.66억)와 매트릭스(원장 기준 80.43억)가 한 화면에서 갈린다.**
그래서 `handleUpload`의 원장 선택을 `findSheetName(wb,'기초재고')||findSheetName(wb,'현재고')`로 바꿨다.
기초재고 시트가 없는 옛 파일은 그대로 현재고로 폴백 — 무회귀.

읽은 시트명을 `result.inventory_sheet`로 남겨 헤더에 `데이터 기준: 기초재고 AM2`로 표시한다.
**데이터에만 의존하는 표시라 모든 열람자가 같은 문구를 본다** (`chAchieveMonth` 계열 함정 회피).

### 같이 넣은 것 — 채널 공백 누락 경고

채널이 비어 Aging에서 통째로 빠지는 금액을 상태줄에 ⚠로 띄운다(자재코드·품목명 상위 3건 포함).
직전 파일에서 8억이 아무 표시 없이 사라졌던 걸 다시 놓치지 않으려는 것.

⚠ **전건을 세면 안 된다.** 기초재고 시트에는 의약품 등 타사업부 원장이 통째로 섞일 수 있고
(26/7 파일 7,269행 1,698억) 그건 채널이 없는 게 정상이다.
`pkgMap`(기준정보 품목군 맵)에 등록된 건기식 자재일 때만 사고로 센다.

### 같이 잡은 잠재 버그

`parseHitSheet._isSummaryRow`가 **정의되지 않은 `chCol`** 을 참조하고 있었다.
`row[nameCol]`·`row[matCol]`이 둘 다 falsy일 때만 세 번째 항이 평가되므로,
**완전 공백 행이 오는 순간 처음으로 `ReferenceError`가 나 업로드 전체가 죽는다.**
26/8 v1.4의 550행이 정확히 그 케이스였다. `pickHitChannel(row)`으로 교체.

### `renderPkgDelta` — 스냅샷 간격이 1개월이 아닐 때

26/7 파일의 기초재고는 6/1 기준이라 26/8(8/1)과 두 달 벌어진다.
`yearmonth` 차이를 보고 머리말을 `전월` → `2개월 전`으로 바꾼다. **숫자·계산은 손대지 않았다.**

### 검증 (3중 교차, 소수점까지 일치)

| 출처 | 값 |
|---|---|
| 시트 컨트롤 총계 `기초재고!AT1` | 7,866,427,939원 |
| 파서 매트릭스/KPI 합계 | **78.66427939억** (수량 4,355,274개) |
| `현황` 시트 8월 트렌드 계 | **78.66427939억** |

- 시트 I+K+N 전체 수량합 = 파싱 수량 4,355,274개 → **버려진 행 없음**
- 7,635행 중 실데이터 503행, 나머지 7,130행은 전부 완전 공백(수식 패딩). B열 빈데 데이터만 있는 행 0건
- 채널 공백 **0행 / 0억**
- PSI 134행, 월 헤더 8~12월, `monthRealLabels` 정상
- 판매계획 246품목, `__layout.ok=true`, 채널 합계 정상 스케일 (26/8 담당자 사고 재발 없음)
- 오차율: 금액 295행 / 수량 297행. `hit.qty.hasPlan=true` — 7월 WAPE 금액 43.6% / 수량 47.3%.
  `계획 0·실적>0` 패턴이 금액(67건)·수량(74건) 양쪽에 같이 있어 수량 구획만의 사고가 아님을 확인

### 배포

- 커밋 `a4f8783` → `npm run deploy` (데이터 280KB gzip 업로드·바이트 검증 통과 + 코드 push)
- 라이브 재확인: `base_label 8/1/26` · `inventory_sheet 기초재고` · KPI 78.66427939억 · 현황 8월 동일
- 라이브 HTML md5 = `public/SOP_LATEST.html` md5 일치 (`50a1e64c…`)
- **화면 숫자 변화: 재고 Aging 총액 80.43억(8/13) → 78.66억(8/1).** 기준 변경이지 사고가 아니다

### 남긴 것

품목군 전월대비가 `6/1(62.20억) → 8/1(78.66억)`, 머리말 "2개월 전"으로 뜬다.
전월 소스인 26/7 파일의 기초재고가 6/1 기준이기 때문. 한 달 전으로 맞추려면 **7/1 기초재고가 든 파일**을
받아 `npm run deploy -- --prev=파일.xlsx`로 지정해야 한다.
26/8부터 기초재고가 당월 월초로 정비됐으므로 **26/9부터는 자동으로 1개월 갭**이 된다.

## 2026-08-16

### 증상: PSI 탭 "순매출 달성율·채널 달성율이 이상하다"

채널별 1월 합계가 **2,086억**(정상 60.5억), 달성율 **4,131%**. 순매출은 브랜드 모드까지 1,735억.

원인은 파서가 채널 블록의 끝(`총계` 행)에서 안 멈추고 계속 내려가, 그 아래 **SKU 상세 블록의 머리글
행(H열 `담당자`)을 채널로 주워담은 것.** 그 행의 월 칸에는 매출이 아니라 년월 숫자(`2026.08`)가
들어 있어 월마다 2,026억이 더해졌다. 순매출은 `getSPBrandNetFactors`가 채널 합계를 역산해 쓰는 구조라
배율이 0.89 → 28.7로 튀면서 같이 무너졌다.

**26/7 파일이 멀쩡했던 건 우연이었다.** 그 파일의 같은 행은 옆칸이 `"판매실적"` 같은 글자라
`Number()`가 0으로 만들어 "전부 0이면 버림" 필터에 걸렸다. 26/8에서 머리글이 2줄→1줄로 합쳐지며
그 자리에 숫자가 들어오자 필터를 통과했다.

시트 양식은 매달 사람이 손보므로 같은 사고가 반복된다. 그래서 증상만 고치지 않고
`rows[34]`·`rows.slice(35)`·`ACTUAL_START_COL=20`·`planCols=[32..36]`·`row[8+i]`·`rows.slice(0,35)` 등
**행/열 고정값을 전부 제거**하고 `findSalesPlanSkuHeader`/`splitSalesPlanHeaderBlocks`로 값에서 찾게 했다.
밀림 시나리오 4종(2·3·5행 삽입, 계획 5→6개월)으로 검증 — 수정 전 코드는 실적월이 `[1~5]`로 조용히
폴백하고 24개월 라벨이 전멸했다.

폴백은 남기되 **경고를 띄운다**: 헤더 미인식 / 순매출 행 없음 / **브랜드 합계 ≠ 채널 합계**.
마지막 것이 이번 사고를 첫날 잡아냈을 검증이다(둘은 같은 총계를 다르게 쪼갠 값이라 반드시 일치).

### 순매출을 0.83 가정치에서 시트 실제값으로

`다이소 ×1.0 / 나머지 ×0.83` 하드코딩이었는데 실제 할인율이 달마다 8~18%로 움직여 26/6에 4.7억
어긋났다. 시트 29행 `순매출(신제품제외)`을 읽어 `나머지 배율 = (순매출총계 − 다이소매출) ÷ (매출총계 −
다이소매출)`로 역산 — 다이소 무할인 규칙을 지키면서 합계가 시트와 **오차 0.0000**.

부수 확인: 시트 `GAP = 순매출 − 사업계획`이므로 **사업계획 목표는 순매출 기준**이다.
매출 모드 달성율은 기준이 어긋나지만 사용자가 "두 기준을 토글로 같이 본다"고 판단해 그대로 뒀다.

### ③탭 채널별 오차율 표 + 오차 금액 차트 신설

시행착오가 있었다. 히트맵 음영 → 제거 요청, 채널별 꺾은선 → 4월 이상치(쿠팡 178%)가 Y축을 0~180%로
끌어올려 나머지 6개월(16~94%)이 플롯의 9~52%에 눌려 폐기. 최종은 **왼쪽 오차율(%) 표 + 오른쪽 오차
금액 발산 막대**.

이 조합을 고른 근거는 **%와 금액의 순위가 정반대**라는 것: 7월 제로샵 107%지만 0.58억(2.1%),
직영몰 37%지만 10.26억(37.9%). %만 보면 손볼 곳을 반대로 짚는다. 막대는 초과/미달로 갈라 그렸다 —
WAPE는 절대값이라 방향이 없어서, 다이소(−4.8억 미달)와 쿠팡(+2.7억 초과)이 오차율은 비슷한데
대응이 반대라는 게 안 보였다.

### 새로 드러난 원본 데이터 문제

셀 클릭 팝업으로 4월 카카오 836%의 정체가 나왔다 — 계획 0.91억인데 실적 6.80억이고, 그중 4.34억이
`직영몰 비타민B_30정`이다. 품목명은 직영몰인데 채널은 카카오. 오차가 아니라 **채널 분류 오류**로 보인다.
`[포장재]` 항목들도 계획 없이 실적만 잡혀 오차율이 수십만 %로 나온다. 둘 다 엑셀에서 고칠 일.


## 2026-08-12

### 증상: 다른 사람이 보면 화면이 다르고 PSI 탭이 깨짐

배포 문제가 아니었음. 라이브 파일과 로컬 파일이 md5까지 동일했고 vendor 에셋도 정상.

원인은 **업로드 데이터가 업로드한 사람의 localStorage(`sopDash_v11`)에만 있었던 것.**
Cloudflare KV에 공유되던 건 `SHARED_STATE_FIXED_KEYS`(회의록/PSI 조정/Action Log/판매계획 셀 수정)뿐이라,
다른 사람은 `UPLOADED=null` 상태로 코드에 내장된 예시 데이터(`PSI`, `MATRIX`, `CH_AMT`, `TREND_DATA`, `SENS_PLAN`)를 봤음.

PSI 탭이 특히 심했던 이유 4가지:

1. 내장 `PSI`는 24행뿐이고 다이소 통합/하위 품목이 없음
2. `monthRealLabels`가 없어 `renderPSI`의 월 헤더가 위치키 그대로 `6월~9월`로 표시 (실제는 8~11월)
3. `sourceMonthlyPsi`가 없어 `calcPsiMonth`가 정확값 대신 비율 추정 폴백(`buy6*0.8`, `plan6*1.03`)으로 계산
4. `getSalesPlanData()`가 null이라 "다음달 판매계획 기준" 재고일수가 대부분 `산출불가`

여기에 KV에서 내려온 공유 PSI 조정값(`DAI-9`, `DAI-33`, `DAI-35`, `DAI-36`)은 내장 PSI에 없는 자재코드라
Action Log에만 남고 표에는 반영되지 않았음.

### 조치 1 — 업로드 데이터 KV 공유

- 신규 Pages Function `functions/api/dashboard-data.js` (KV 키 `dashboard:live`)
  - `GET` 페이로드 / `GET ?meta=1` 메타만 / `PUT` 저장(관리자) / `DELETE`(관리자)
  - 관리자 인증은 `x-admin-password` 헤더 (본문이 바이너리라 body에 못 넣음)
  - 메타(`uploaded_ts`/`uploaded_at`/`base_label`)는 `x-sop-*` 헤더로 전달, 한글 때문에 전 구간 URI 인코딩
  - 상한 20MB (KV 값 상한 25MB 아래), KV 없으면 D1에 base64로 폴백
- 클라이언트: `packDashboardData`/`unpackDashboardData`(gzip), `saveDashboardData`, `loadSharedDashboardData`,
  `shareCurrentDataNow`, `noticeIfNoData`
- `handleUpload`에 `result.uploaded_ts=Date.now()` 추가 — `uploaded_at`은 `toLocaleString`이라 비교 불가였음
- 최신본 판정: 서버 공유본 vs 로컬 `uploaded_ts` 비교, 더 최신인 쪽 채택.
  타임스탬프 없는 구버전 로컬본은 공유본이 이김.
- 부팅 순서: `loadSharedDashboardData`/`loadSharedState`를 **await** 하도록 변경.
  기존엔 `runStep`이 async 함수를 await 없이 호출해서 데모 데이터가 먼저 그려지고 나중에 덮이는 경합이 있었음.
  `BOOT_RENDER_DONE` 플래그로 `refreshSharedStateViews`의 조기 부분 렌더도 차단.
- 관리자 모드가 아닌 채로 업로드하면 `⚠ 이 브라우저에만 저장됨` 경고.
- 로컬·서버 둘 다 데이터가 없으면 내장 예시값이 그려진다는 사실을 배너로 명시(`noticeIfNoData`).
- 압축률 실측 9.5배 (1,051,603B JSON → 111,145B gzip).

### 조치 2 — 웹폰트 self-host

- `vendor/PretendardVariable.woff2` (2.0MB, variable). static 5개 웨이트(400/600/700/800/900)면 3.8MB라 variable이 더 작음.
- `body` 폰트 스택 앞에 `'Pretendard Variable'` 추가.
- `UI_FONT_STACK` 상수 신설. Chart.js 기본 폰트(`Chart.defaults.font.family`)와
  캔버스 배지 `ctx.font` 6곳이 각각 `Apple SD Gothic Neo` / **`Arial`**(한글 글리프 없음)로 갈려 있던 것을 통일.

### 조치 3 — "숫자가 다르면 안 된다" 재점검에서 추가로 나온 2건

데이터 전달만 맞추는 걸로는 부족했고, 화면 숫자가 갈리는 경로가 2개 더 있었음.

**(1) ③ 판매계획 오차 분석 탭의 월 드롭다운이 사람마다 달랐음**

`chAchieveMonth`는 HTML에 `<option value="5월" selected>`로 박혀 있고,
데이터 최신월로 자동 전환하는 코드는 **`handleUpload` 안에만** 있었음.
→ 업로드한 사람은 7월(최신월) 오차분석을, 다른 열람자와 새로고침한 본인은 **5월**을 봤음. 완전히 다른 숫자.
→ `syncAchieveMonthToData()`로 분리하고 업로드·`loadFromStorage`·`loadSharedDashboardData` 세 경로 모두에서 호출.

**(2) 업로드한 사람만 JSON을 안 거친 원본 객체를 보고 있었음**

`UPLOADED=result`로 파서 원본을 그대로 잡고 있어서, JSON을 거쳐 받는 다른 열람자와
`NaN`/`Infinity` → `null`, `undefined` 키 소실, `-0` → `0` 만큼 값이 갈릴 수 있었음
(0으로 나누는 계산이 있는 `rate6`, `dp`, `dr` 등이 실제 위험 지점).
→ `UPLOADED=JSON.parse(JSON.stringify(result))`로 업로드 직후부터 모두 같은 정규화본을 쓰게 맞춤.
구조적으로 차이가 날 수 없게 만든 것이라 파서를 나중에 고쳐도 안전함.

참고: `monthSlider`(Sensing M+N)는 HTML 기본값 0 = 부팅 렌더 `renderSensing(0)`이라 원래 일치했음.

### 조치 4 — 엑셀 → KV 업로드 CLI (`tools/publish-data.mjs`)

사용자가 "리포에 엑셀 올린 다음 배포해달라고 하겠다"고 해서 만듦.
**배포로는 데이터가 안 올라간다** — 배포는 `public/` 파일만 올리고 숫자는 KV에 있으며, 엑셀은 `.gitignore` 대상.
그래서 파싱 후 KV에 직접 PUT하는 CLI가 별도로 필요했음.

- `npm run publish:data -- <파일.xlsx>` / `ADMIN_PASSWORD` 환경변수 또는 gitignore된 `.env.local`
- **파서를 새로 구현하지 않았음.** `vendor/xlsx.full.min.js` + `SOP_LATEST.html`의 인라인 스크립트를
  vm에 로드하고, `FileReader`를 스텁해서 **실제 `handleUpload()`를 그대로 호출**한다.
  별도 파서를 짜면 브라우저 업로드와 숫자가 반드시 어긋나므로 이 구조를 유지할 것.
- `fs.readFileSync(p).toString('binary')`(latin1) = 브라우저 `FileReader.readAsBinaryString` 출력과 동일.
- 업로드 후 다시 GET해서 바이트 일치를 자동 검증하고, 다르면 exit 1.
- 검증: 합성 엑셀(PSI 12행)로 로컬 wrangler 상대 실전 실행 성공.
  월 라벨을 헤더에서 `8~12월`로 정확히 읽었고(PSI 시트 함정 구간), gzip 업로드 → 되받기 바이트 일치 확인.

### 조치 5 — `npm run deploy` 한 방으로 통합

사용자 요구: "웹에서 업로드 안 하고, 여기에 엑셀 올리고 배포하면 되게."
조치 4의 `publish:data`는 데이터만 올리는 별도 명령이라 두 단계였음. 하나로 합침.

- 공용 로직을 `tools/lib/publish.mjs`로 분리 (`parseExcel`/`publishParsed`/`findLatestExcel`/`readPassword`)
- `tools/deploy.mjs`: ① 최신 `.xlsx` 자동 탐색 → ② 파싱·KV 반영·되받기 검증 → ③ 라이브와 코드가
  다를 때만 `git push` 후 Cloudflare 반영까지 폴링 대기
- **파일명을 입력할 필요 없음** — VS Code 탐색기에 드래그해서 떨구기만 하면 가장 최근 파일을 집는다
- 비밀번호는 gitignore된 `.env.local`에 1회 등록 (`.gitignore`에 규칙 추가 확인함)
- 대시보드 스크립트의 `console.info`가 CLI에 새던 것을 캡처해서 경고만 정리해 출력
- 옵션: `--dry-run` / `--data-only` / `--code-only` / `--base`

검증 (로컬 wrangler + KV, 합성 엑셀 PSI 12행):
- `--dry-run` → 파싱만, 월 헤더 8~12월 정확히 읽음
- 실제 반영 → gzip 업로드 후 되받기 바이트 일치 확인
- 비밀번호 없을 때 스택 트레이스 대신 안내 문구 출력
- 코드 배포 단계는 라이브와 동일하면 건너뜀

### 조치 6 — 오차율 분석 시트 [수량] 구획 반영

사용자가 오차율 분석 시트에 `[매출금액]`/`[순매출]`/`[수량]` 구획을 만들고 수량 데이터를 넣음
(월 컬럼 24개 → 60개, 범위 `B1:AD961` → `B1:BR991`).

- `parseHitSheet`에 구획 라벨 행 탐지 추가. `[수량]`은 별도 맵, `[순매출]`은 건너뜀(사용자가 불필요하다고 명시).
  라벨 없는 옛 파일은 전 구간 금액 → 무회귀.
- 집계 로직을 `aggregate()`로 추출해 금액(억원)·수량(개) 두 번 호출. 계산식은 동일(WAPE).
- **기존 화면의 금액/수량 토글은 `금액 ÷ 단가` 추정치였다.** 이제 같은 `월|채널|자재`의 실측 수량을
  금액 행에 `qtyPlan`/`qtyActual`로 붙이고, `realQty()` → 추정 순으로 폴백. 추정치는 `~` 접두사와 tooltip 표시.
- 수량 판매계획(AU~BF)이 아직 비어 있어 수량 오차율은 산출불가. `hasPlan` 플래그를 둬서
  나중에 채우면 코드 수정 없이 켜지게 함.

검증: vm 하네스 13개 통과 (실측 우선, 계획 0은 없음 취급, 계획 채워진 가정, 7월 파일 무회귀).

### 조치 7 — 회의록 월별 자동 보관 + 월 선택 즉시 조회

회의록은 `saveMeetingMemo()`가 단일 값을 덮어쓰는 구조라 누적되지 않았고,
`현재 월 보관` 버튼을 안 누르면 다음 달에 그대로 사라졌다.

- `queueSharedStateSave()`에 `queueAutoArchive()`를 붙여, 입력이 멈추면(8초) 현재 기준월
  보관본을 자동 갱신. live 저장(450ms)보다 길게 잡아 KV 쓰기 횟수를 억제.
- 자동 보관 결과를 `#archiveAutoNote`에 표시하고, 실패 시 수동 버튼을 안내.
- 보관본 드롭다운에 `onchange="loadSelectedArchive()"` — **월을 고르면 그 달 회의록이 즉시 표시**.
  `조회` 버튼 제거, 빈 선택 시 alert 대신 미리보기 닫기.

검증: 로컬 wrangler + KV로 10개 통과 (자동 보관, 달 바뀌어도 이전 보관본 보존,
신규 월 생성, 목록 반영, 선택 즉시 렌더, 비관리자 미실행).

### 조치 8 — SKU 상세 팝업 EA 표기 · 판매/원가 병기 · 표 가독성

- 미소진 Risk 문구와 모달 수량(현재수량·누적계획·예상 잔여수량)에 `EA` 표기.
  `formatQty`는 차트·PSI 공용이라 건드리지 않고 `formatQtyEa()`를 따로 뒀다.
- **예상 잔여금액이 판매단가(SF 계획단가) 기준이었다.** 재고 Aging 매트릭스 금액은 원가 기준이라
  같은 화면에 기준이 두 개 섞여 있었음. 원가 시트의 SKU별 `순매출÷원가` 배율로 나눈 원가 환산을
  같이 표시(`판매 152.8백만원 · 원가 55.2백만원`). 원가 시트에 없는 SKU는 전사평균 배율(3.25)을 쓰고 `*` 표시.
  실측 배율 편차가 2.04~4.21로 커서 병기가 유의미함.
- **표 행높이의 원인은 조치의견 textarea `min-height:78px`이었다.** 평소 26px, 포커스 시 76px로 펼침.
  셀 여백 th 9→6px / td 9→4px, 오차율 태그 래퍼 27→20px. 행높이 약 96px → 34px.
- 총계 행 배경 `#eef0f9 → #dfe3f5` + 좌측 네이비 강조바, 짝수행 `#fafbfd` 얼룩, 호버 `#eef2ff`.

### 확인만 하고 코드는 안 바꾼 것 (이미 되어 있었음)

- **판매계획 보정 대상 리스트는 이미 전품목이다.** `getVisibleAdjustmentRows()`는 채널 필터만 걸고
  보정 불필요 행도 흐리게 표시할 뿐 빼지 않는다. 7월 236행 / 6월 302행, 필터로 빠지는 행 0개.
  적어 보인 원인은 조치의견 칸 높이였음.
- **다이소 통합도 이미 적용 중이다.** `remapHitRowDaiso()`가 개별코드를 통합코드로 치환해 채널+통합코드로 합산.
  7월 기준 다이소 39행 = 고유 통합코드 39개, 개별코드 잔존 0건. 품목명 옆 `통합` 태그 표시.
  예: DAI-1 루테인 ← 7302027(0.20억) + 7302241(0.64억) = 0.84억
- **오차율분석 시트는 PSI가 아니라 SF를 따라간다.** 고유자재 105개가 SF에 100% 포함(∩SF 105/105),
  PSI와는 58/105만 겹침. 다이소는 개별코드로 들어있고 화면에서 통합됨.

### ⚠ 미해결 — SF 판매계획 열이 한 달 밀림

자재 7300121 수량 열(AQ~AU) 대조:

```
7월파일 헤더  2026.07  2026.08  2026.09  2026.10  2026.11
7월파일 값       5218     5298     6203    10227    10138
8월파일 헤더  2026.08  2026.09  2026.10  2026.11  2026.12
8월파일 값       5298     6203    10138    10227        0
```

PSI 기준 실제 8월 판매계획은 6203인데, SF `AQ`(헤더 2026.08)에는 7월 값 5298이 들어 있고
6203은 `AR`에 있다. 두 파일 모두 같은 방향으로 어긋나 있어 일회성 실수가 아님.

코드에도 별개 문제가 있다. `parseSalesPlanSheet`가 `month:6+i`로 하드코딩하는데,
시트에서 실제 월을 읽는 `readRealMonths()`가 이미 있고 `groups.planMonths`에
`[8,9,10,11,12]`로 정확히 담겨 있다. **지금은 두 오류가 상쇄돼 `getNextPlan`의 `idx=nextMonth-6`이
우연히 맞는다. 한쪽만 고치면 오히려 틀어진다.**

영향 범위: 보정 대상 리스트의 차월 권장계획. 리스트 자체(계획/실적/오차율)에는 영향 없음.
사용자가 시트를 고칠지 코드를 고칠지 정해야 진행 가능.

### 남은 이슈 (이번에 안 건드림)

- `body{min-width:1280px}` — 좁은 화면에서 가로 스크롤/레이아웃 깨짐. 사용자가 이번엔 제외하기로 함.
- 관리자가 데이터를 새로 공유해도 열려 있는 다른 사람 탭은 자동 갱신되지 않음 (새로고침 필요).
- 스냅샷은 여전히 IndexedDB 로컬 전용이라 공유되지 않음.

### 검증

- `functions/api/dashboard-data.js`: 로컬 wrangler pages dev + KV 바인딩으로 17개 API 테스트 통과
  (권한 403, 빈 상태 404, gzip 왕복 바이트 일치, 한글 메타 헤더 왕복, DELETE).
- `SOP_LATEST.html`: CLAUDE.md의 Node 재현 하네스로 21개 클라이언트 테스트 통과
  (신규 방문자 공유본 수신, `monthRealLabels`/`sourceMonthlyPsi`/`sales_plan` 보존,
  `getPSIData()`가 내장 데모 대신 공유본 반환, 최신본 판정, 비관리자 차단, 폰트 스택).
- **숫자 동일성 테스트 12개 통과** (`numtest.mjs`): 관리자 샌드박스와 열람자 샌드박스를 각각 독립
  localStorage/DOM으로 띄우고, 0 나눗셈·NaN·undefined·-0을 일부러 섞은 300행 데이터셋으로
  `UPLOADED` 직렬화 바이트 일치, `calcPsiMonth`/`getUnitPrice` 실계산 **7,200셀** 일치,
  월 드롭다운 일치, 열람자 새로고침 후 일치, 재업로드 후 재일치까지 확인.
- 하네스 스크립트: scratchpad `apitest.mjs`, `clienttest.mjs`, `numtest.mjs`.

### 검증 못 한 것

실제 엑셀(`.gitignore` 제외)이 리포에 없어서 **진짜 원본 데이터로는 端to端 확인을 못 했음.**
배포 후 관리자로 실제 엑셀을 올린 뒤, 시크릿 창으로 같은 URL을 열어 PSI 탭 월 헤더와
몇 개 품목 숫자를 눈으로 대조하는 절차가 한 번 필요함.

## 2026-07-13

### Cloudflare Pages / KV

- 운영 사이트: `https://s-op-gungi.pages.dev`
- Cloudflare 계정 ID: `14c780e41fc86dde4101283fb427b14e`
- Pages 프로젝트: `s-op-gungi`
- KV binding name: `SOP_STATE`
- 실제 KV namespace ID: `05ce4e26ad784fde95ecad137c60b2ee`
- 기존 문제였던 `Missing Cloudflare binding`은 `wrangler.toml`의 잘못된 KV ID 때문에 발생했고, 실제 ID로 수정 후 운영 배포 완료.

### 배포 방법

- 운영 배포 명령:

```bash
npm run release:live
```

- `package.json`에 Cloudflare 계정 ID와 Pages 프로젝트명을 고정해둠.
- Cloudflare 로그인이 필요한 새 환경에서는 먼저 실행:

```bash
npx wrangler login
```

### 공유 상태 저장

- API: `/api/shared-state`
- `live` 키는 현재 회의 입력 상태 저장용.
- 월별 보관본은 `archive:YYYY-MM` 키로 저장.
- 보관 목록은 `archive:index` 키로 관리.
- 2026-07 S&OP 보관본 생성 완료.

### 월별 S&OP 보관 흐름

- 회의록 탭 상단에 기능 추가:
  - `현재 월 보관`
  - `과거 보관본 조회`
  - `새 달 시작`
- 권장 운영:
  - 회의 종료 후 `현재 월 보관`
  - 다음 달 시작 전 `새 달 시작`
  - 과거 회의는 드롭다운에서 조회

### 관리자 모드

- 기본은 읽기 전용.
- 상단 `관리자 모드` 버튼에서 비밀번호 입력 후 수정 가능.
- Cloudflare Pages secret 이름: `ADMIN_PASSWORD`
- 실제 비밀번호 값은 GitHub 문서에 기록하지 않음. Cloudflare Pages secret에서 관리.
- 비밀번호 없는 저장 API 요청은 403으로 차단됨.
- 관리자 모드에서만 가능한 작업:
  - 회의록 수정
  - 담당 의견 수정
  - PSI 입고 조정
  - 월별 보관
  - 새 달 시작
  - 스냅샷 저장/불러오기/삭제

### PSI 계산 변경

- PSI 재고일수 기준을 전체 월에 대해 다음 기준으로 통일:

```text
해당 월 재고일수 = 해당 월 기말재고 / 다음 월 판매계획 수량 * 30
```

- 예:

```text
8월 재고일수 = 8월 기말재고 / 9월 판매계획 수량 * 30
```

- 적용 범위:
  - 일반 PSI 품목
  - 다이소 통합 품목
  - 다이소 원본 하위 품목
  - 판매계획 기준 재고일수
  - 진척율/보조 기준 재고일수
  - 정렬
  - 위험/정상 집계
  - 판매 증감 시뮬레이션

### 다이소 통합 PSI

- 다이소 품목은 `다이소마스터` 기준으로 하위 자재를 통합코드에 합산.
- 공급 시트가 있으면 다이소 입고는 공급 시트 기준으로 반영.
- 통합 품목의 진척율 기준 예상 재고도 공급 반영 입고 기준으로 재계산하도록 수정.

### PSI 입력 UX

- PSI 입고 조정 입력 후 `Enter`를 누르면 즉시 반영.
- 입력칸 밖을 클릭해도 기존처럼 반영.

### 주의

- 최신 엑셀을 업로드하지 않으면 내장 기본 데이터 또는 이전 로컬 데이터 기준으로 보일 수 있음.
- 업로드 데이터는 브라우저 localStorage에 저장되고, 회의록/PSI 조정/의견은 Cloudflare KV에 저장됨.
- 배포 버전 URL은 확인용이고, 평소 공유/사용 주소는 운영 URL `https://s-op-gungi.pages.dev`만 사용.
