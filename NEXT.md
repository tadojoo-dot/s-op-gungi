# 다음에 이어서 할 일

> 세션을 껐다 켜도 이 파일만 읽으면 바로 이어갈 수 있게 정리한 문서.
> 자세한 배경은 `WORK_LOG.md`(2026-08-12), 코드 구조·함정은 `CLAUDE.md` 참고.
> **작업을 끝내면 이 파일을 갱신할 것.**

## 지금 상태 (2026-08-12 기준)

- 운영 사이트: https://s-op-gungi.pages.dev — 코드·데이터 모두 최신 배포 완료
- 라이브 데이터: **26년 8월 파일 v1.1** (`기준일 8/13/26`, PSI 133행, 판매계획 245품목) — 2026-08-13 반영
- 배포 방법: 엑셀을 리포 루트에 넣고 `npm run deploy` (파일명 불필요, 최신 `.xlsx` 자동 탐색)
- 관리자 비밀번호: gitignore된 `.env.local`의 `ADMIN_PASSWORD`
  ⚠ **Codespace를 새로 만들면 이 파일이 없다.** 사용자에게 다시 만들어 달라고 할 것
  (VS Code 탐색기에서 `.env.local` 생성 → `ADMIN_PASSWORD=비밀번호` 한 줄)

## 방금 끝낸 것 ⓪ (2026-08-13) — PSI 표 0건일 때 원인 안내

"플랜트=횡성 + 다이소 전체 = 빈 화면" 문의에서 나온 작업. **버그가 아니라 기본 필터였다.**

- 원인: 3평판 필터 기본값이 `3평판 있는 품목만`인데 횡성 다이소 원본 14건은 3평판이 전부 0 → 전멸
- **횡성 코드(`9401xxx`)는 3평판·당월실적·판매계획·입고계획이 모두 0이고 재고만 있다.**
  다이소 품목의 2번째 생산처로 나중에 붙은 코드라 판매는 전부 향남 코드(`7302xxx`)로 나간다.
  그래서 필터를 풀어 보이게 해도 전 행이 회색 "산출불가"다 — 재고일수 분모가 없음
- 필터가 가리는 재고: 다이소 통합 0개 / 향남 0개 / 일반 PSI 8.8만개 / **횡성 79.4만개** (횡성만 이상치)
- 조치: 필터 로직을 `applyPsiFilters(값묶음)` 순수 함수로 분리하고, 0건일 때 필터를 하나씩 빼보며
  (leave-one-out) 살아나는 필터를 짚어 준다. **화면 기본값인 필터를 맨 위로 정렬** — 사용자가 직접 고른
  플랜트(해제 시 47건)가 위에 오면 정작 범인인 3평판이 묻힌다
- 사용자 선택을 코드가 자동으로 되돌리는 안(A안)은 뺐다. `CLAUDE.md`의 "UI 컨트롤을 프로그램으로 바꿔서
  사람마다 다른 화면" 함정과 같은 계열이고, 풀어도 회색 행만 나와서 실익도 없다
- 회귀검증: 필터 1,458개 조합에서 리팩터 전/후 결과 완전 일치 (scratchpad `emptystate.mjs`)

### 후속 — 플랜트별 분해로 이어짐 (아래 항목)

"통합으로도 보고 플랜트별로도 보고 싶다"는 요청으로 바로 이어졌다. 배분 규칙이 필요하다고 했던 건
3평판을 플랜트에 **나눠주려** 할 때 얘기였고, **분모를 통합으로 두면 나눌 게 없어져서** 해결됐다.

## 방금 끝낸 것 ⓪-2 (2026-08-13) — 다이소 플랜트별 분해

플랜트 필터를 고르면 다이소 통합 행이 **그 플랜트 몫**으로 바뀐다. 안 고르면 지금까지처럼 통합.

- 규칙과 기각한 대안은 `CLAUDE.md`의 **"다이소 플랜트 분해 규칙"** 에 정리 — 반드시 거기부터 읽을 것
- 핵심: `플랜트 재고일수 = 플랜트 기말재고 ÷ 통합 다음달 판매계획 × 30`.
  **플랜트별 재고일수 합 = 통합 재고일수**가 정확히 성립 (전건 검증)
- 신호등·3평판은 통합 기준(플랜트별로 걸면 향남 46일/횡성 103일이 각각 미달인데 합치면 149일로 정상)
- 진척율·3평판 기준 보조 열은 플랜트 분해에서 `—`. 입고 조정 input도 잠금
- 다이소 원본(개별 코드) 뷰도 같이 고침: 자체 계획이 0인 코드 25건이 소속 통합 수요를 분모로 빌려
  재고일수가 산출된다(전엔 전부 회색). **자체 3평판이 있는 코드는 덮어쓰지 않는다** — 0건 확인
- 검증 (scratchpad `plantview.mjs`, `final.mjs`):
  - 향남+횡성 = 통합 (기초·입고·판매·기말) 불일치 **0건**
  - 재고일수 가산성 불일치 **0건**
  - 플랜트 미선택 경로 216조합 회귀 불일치 **0건**
  - 통합 재고 3,265,035 = 향남 2,471,455 + 횡성 793,580 ✅
  - 금액 토글(수량/원가/매출) × 플랜트 조합에서 undefined/NaN 노출 없음

## 방금 끝낸 것 ① (2026-08-13) — ③탭 오차율 트렌드 차트 실측화 ✅ 배포됨

`renderHitTrend()`가 코드 상수 4개월(45.5/47.8/49.1/50.0)을 그리던 걸 실측으로 교체했다.

- 새 함수 `buildHitTrend()` — `hit.byMonth`를 `remapHitRowDaiso`+`aggregateHitRows` 거쳐 월별 WAPE 산출
- **파서의 `hitAccByMo`를 그냥 쓰지 않은 이유:** 그건 다이소 개별코드 raw 집계라 KPI 타일과 숫자가 어긋난다
  (전체 1월 53.8 vs 48.8, 6월 36.2 vs 26.5). 통합코드로 합치면 같은 품목 내 초과/미달이 상쇄되기 때문.
  화면의 KPI 타일·보정 리스트는 전부 remap+aggregate 경로라 **막대도 같은 경로로 맞췄다** — 월/채널 조합 5건 교차검증 일치 확인
- 판매계획이 빈 달(26/8의 7~12월)은 막대를 아예 안 그린다. 0%로 그리면 "오차 없음"으로 읽혀 정반대 의미가 됨
- 채널 필터(`fhc`)가 전 구간에 걸린다 (전엔 마지막 막대만 반응)
- y축 `max:100` → `suggestedMax:100` (4월 157.7%가 잘리던 문제)
- 라벨 `(MAPE)` → `(WAPE)` 오기 수정. 실제 산식은 처음부터 WAPE였다
- 업로드 데이터 없으면 `buildHitTrend()`가 null → 기존 상수 폴백 유지 (무회귀)
- 검증 하네스: scratchpad `trend.mjs`(월별/채널별 값), `trend2.mjs`(KPI 타일 교차검증 + 폴백)

## 방금 끝낸 것 ② (2026-08-13) — PSI 전 품목 현황 수량/원가/매출 토글 ✅ 배포됨

표 상단에 `수량 / 원가 / 매출` 버튼 3개(`setPsiUnitMode`). 기초·입고·판매·기말 4개 열만 백만원으로 환산하고
3평판·재고일수·신호등은 항상 수량·일수 기준(판정 결과 불변).

- 원가단가 3단 폴백(`getPsiPriceMap`): ① 현재고 재고금액÷재고수량 → ② 다이소 통합코드는 하위 `componentCodes` 재고 가중평균 → ③ 원가시트 배율 역산
  - **②가 핵심.** 통합 SKU(DAI-xx)는 현재고 시트에 그 코드가 없어 원가단가가 전부 0인데, 재고 대부분(326만개)이 여기 몰려 있다. ②를 빼면 원가 커버리지가 수량가중 24%로 떨어짐
- 매출단가는 기존 `getUnitPrice()` 그대로
- 26/8 파일 검증: 재고>0인 103 SKU 중 원가 103건(100%) / 매출 93건(90%, 수량가중 87%). 못 찾으면 `—`
- 전체 기초재고 = 수량 431만개 / 원가 73.0억 / 매출 278.4억
- 금액 모드에선 입고 조정 input이 읽기전용 텍스트로 바뀐다(금액→수량 역산 반올림 오차 방지). 정렬도 금액 기준으로 같이 전환됨
- 검증 하네스: scratchpad `pricecov*.mjs`(커버리지), `psiunit.mjs`(3모드 렌더), `spot.mjs`(환산값 손계산 대조)

## 방금 끝낸 것 ③ (2026-08-13) — PSI 전 품목 표에서 차월 이후 "3평판 기준" 열 제거 ✅ 배포됨

당월(8월)만 `진척율 기준` 보조 2열(기말재고·재고일수)을 유지하고, **차월 이후(9·10·11월)의 `3평판 기준`
보조 2열은 삭제**했다. 사용자 요청.

- **월 블록 폭이 달라졌다: 당월 7칸 / 이후 5칸** (전엔 전부 7칸). 총 컬럼 36 → 30
- 새 헬퍼 `psiMonthSpan(m)` / `psiMonthCellOffset(mi)` / `psiTotalCols()` (`PSI_MONTHS` 바로 아래)
  - ⚠ `recalcPsiRow`가 `bi=7+mi*7`로 셀 인덱스를 세고 있었다 — **이걸 안 고치면 입고 조정 후
    두 번째 달부터 엉뚱한 칸에 숫자가 박힌다.** 지금은 `psiMonthCellOffset(mi)` 사용
  - 보조 기준이 당월(진척율=실적 기반)만 남아 `recalcPsiRow`에서 보조 셀 갱신 자체가 불필요해짐
- `month-end` 구분선 클래스를 차월 이후엔 재고일수 열로 옮김(그 열이 월 블록의 마지막이 됨)
- 비고 산식 문구·플랜트 안내 문구에서 3평판 기준 설명 삭제
- 검증 (scratchpad `repro_psi_cols.mjs`): thead 총폭 30 = tbody 133행 전부 30칸,
  헤더 `3평판 기준` 0개 / `진척율 기준` 1개, 월별 오프셋(7/14/19/24)에서 기초·기말·재고일수 정위치 확인
- 배포 완료 (커밋 `daf9089`). 같이 커밋하며 `wrangler.toml` 끝에 실수로 들어가 있던 `ㅊ` 한 글자도 되돌렸다

## 방금 끝낸 것 ④ (2026-08-13) — ③탭 오차율 트렌드 차트 신설 + 금액/수량 토글

**차트가 화면에 없었다.** `buildHitTrend()`/`renderHitTrend()`는 있는데 `<canvas id="hitTrendChart">`가
git 히스토리 전체에 한 번도 없었다 — `renderHitTrend()`는 `renderHitKpis()`만 하고 null 캔버스에서 리턴.
①번 항목("트렌드 차트 실측화")은 **계산 함수만 만들고 화면엔 못 붙인 상태**였던 것. 이번에 캔버스를 신설했다.

- 위치: ③탭 KPI 타일 바로 아래 전체폭 (`.grid-5-5` 위), 헤더 우측에 `금액 / 수량` 버튼
- `hitTrendUnit` + `setHitTrendUnit` + `syncHitTrendUnitUI` (기존 `setInsightUnit`/`setHTUnit`과 같은 패턴)
- 수량 소스는 `hit.qty` — `hasPlan=false`면 버튼을 잠그고 "산출 불가" 안내를 띄운다.
  **선택 자체는 코드가 바꾸지 않는다** (`chAchieveMonth` 함정과 같은 계열). 버튼 상태 동기화는
  업로드 경로가 아니라 `renderHitTrend()` 안에 둬서 모든 열람자가 같은 화면을 본다
- 폴백 상수(`HIT_TREND_DEFAULT`)는 금액 기준이라 **수량 모드에선 쓰지 않는다** — 쓰면 가짜 숫자가 나온다
- 검증 (scratchpad `repro_hittrend.mjs`): 금액 1월 48.8~7월 36.5% / 수량 1월 36.6~7월 24.7%,
  채널 필터(다이소) 반영, `hasPlan=false` 잠금, 업로드 없음 시 금액 폴백 유지(무회귀) 전부 확인

## 방금 끝낸 것 ⑤ (2026-08-13) — PSI 표 폭 줄이기 (너비만)

가독성 개선 논의 중 사용자가 **"너비만"** 으로 범위를 한정했다. 나머지(sticky 고정열, 신호등 pill,
요약/상세 2단 뷰, 소계 행, 12월 추가)는 **논의만 하고 착수하지 않음** — 다시 꺼낼 때 아래 참고.

- 고정 6열 min-width 축소: 자재코드 86→72 / 품목명 210→178 / 품목군 72→54 / 구분 96→76 /
  플랜트 110→74 / 소진판단 74→60 (합 136px 절약)
- ⚠ **핵심은 `.psi-tbl.monthly{min-width:2700px}` → `2320px`.** 이걸 안 내리면 줄인 폭을
  월 열들이 나눠 가져서 **총 폭이 그대로**다. 열을 6개 지웠는데도 표가 안 좁아졌던 이유가 이것
- 구조 회귀 없음 확인 (thead 총폭 30 = tbody 133행 전부 30칸)

### 논의만 하고 보류한 것 (사용자 요청 시 재개)

- **월 5개월(12월까지)이 데이터 상한.** `sourceMonthlyPsi` 블록이 5개뿐이고,
  12월은 분모가 될 1월 판매계획이 시트에 없어 재고일수·신호등이 영구 회색
- 가독성 후보: ① 자재코드·품목명 `position:sticky;left:0` ② 재고일수를 신호등 색 pill로
  ③ 기초/입고/판매 흐리게 + 기말·일수 진하게 ④ 월 블록 교대 배경
  ⑤ 요약(월 2칸)/상세(월 헤더 클릭 시 5칸) 2단 뷰 → 5개월 넣어도 폭이 지금 절반
- **소계 행**: `thead` 맨 아래 sticky. ⚠ 재고일수는 평균 금지 — `Σ기말 ÷ Σ다음달계획 × 30`으로
  재계산해야 한다. 3평판은 다이소 통합/하위가 값을 공유해 필터 조합에 따라 이중계상 위험
- **횡성(9401xxx) 매출단가가 `—`인 원인 확인됨**: `getUnitPrice`가 ①SF 계획단가 ②PSI 계획금액÷수량
  두 경로뿐인데 횡성은 SF에도 PSI에도 자기 행이 없다(재고만 있고 판매는 향남 코드로 나감).
  원가는 현재고 재고금액÷수량이라 나온다. 고치려면 원가단가의 "통합 하위품목 차용"과 대칭으로
  **형제(향남) 코드의 매출단가를 빌려오는 폴백**을 붙이면 된다 (`daiso_code_map` 이미 있음)

## 합의된 코드 작업 대기열

**비어 있다.** 사용자가 새로 요청하기 전까지 착수할 코드 작업 없음.
남은 건 아래 "사용자가 해야 진행되는 것"과 "미해결 이슈"뿐.

## 사용자가 해야 진행되는 것 (코드 작업 아님)

| 할 일 | 안 하면 |
|---|---|
| PSI 시트 **12월 블록 판매계획** 채우기 | 표시 마지막 달(11월) 재고일수 분모가 없어 **신호등 133건 전부 회색 "산출불가"** |
| PSI 시트 **10~12월 입고(공급계획)** 채우기 | 입고 SKU가 10월 12건 / 11월 1건 / 12월 0건. 기말재고가 실제보다 낮아 음수 기말 16건(11월) |

**v1.1(8/13)에서 해소된 것** — 오차율분석 7월 판매계획(228건 76.8억) ✅ / [수량] 판매계획 AU~BF ✅
(`hit.qty.hasPlan=true`, 수량 WAPE 43.0% 산출) / 평균출하(3평판) 갱신 ✅ / SF 26년 7월 실적 73.4억 ✅

## 미해결 이슈

### 1. SF 판매계획 열이 한 달 밀림 (사용자 결정 대기)

```
8월파일 SF AQ(헤더 2026.08) = 5298  ← 7월 값
        SF AR(헤더 2026.09) = 6203  ← 실제 8월 값 (PSI와 대조해 확인)
```

7월 파일도 같은 방향. 코드에도 별개 문제가 있다 — `parseSalesPlanSheet`가 `month:6+i` 하드코딩인데,
시트에서 실제 월을 읽는 `readRealMonths()`가 이미 있고 `groups.planMonths`에 정확히 담겨 있다.
**지금은 두 오류가 상쇄돼 `getNextPlan`의 `idx=nextMonth-6`이 우연히 맞는다. 한쪽만 고치면 틀어진다.**

영향 범위는 보정 리스트의 **차월 권장계획 한 칸뿐**. 계획·실적·오차율 본체는 오차율분석 시트에서 온다.
→ 시트를 고칠지 코드를 고칠지 사용자가 정해야 함. 우선순위 낮음.

### 2. 당월실적 시트가 0품목 — **원인 확인됨 (2026-08-13), 수정 대기**

`actual_unit`이 계속 0건. 원인은 **시트 범위가 `B1`부터 시작인데 `parseActualQtySheet`가 A열 기준으로
인덱스를 잡은 것** — PSI 시트와 똑같은 오프셋 함정이다.

```
코드 주석: X=23 자재, AZ=50 매출금액, BB=52 매출수량
실제 값  : idx23="제품군", idx50="매출수량", idx52="반품금액"(=0)
```

`qty<=0`에 전건이 걸러진다. 헤더 행에서 열 이름으로 인덱스를 찾게 고치면 된다.
영향: PSI 시뮬레이션 단가 폴백(`실적단가`), ③탭 오차 인사이트 수량 추정(`qtyFromAmountM`).

### 2-1. 엑셀 시트 정리 (2026-08-13 조사)

`실적수량` 시트는 **코드도 안 읽고 수식 참조도 0건** → 지워도 안전.
반면 `평균출하`는 코드는 안 읽지만 **PSI!F열(3평판)이 683건 수식으로 참조** → 지우면 3평판이 `#REF!`,
`원가`는 코드가 직접 읽음(매출환산 열) → 둘 다 삭제 금지.

### 3. 소진계획 메모에 월 개념이 없음

`data-plan-key`가 `AGING|채널|구간|SKU` / `PSI|자재코드` 형식이라 월 구분이 없다.
지난달 메모가 이번 달 행에 그대로 남는다. 숫자에는 영향 없음.

## 세션 이어가기

```bash
claude --continue     # 이 폴더의 가장 최근 대화 이어서
claude --resume       # 대화 목록에서 골라서
```

대화 기록은 `~/.claude/projects/-workspaces-s-op-gungi/*.jsonl`에 있다.
**Codespace 안에만 있으므로 Codespace를 삭제하면 사라진다** (GitHub 기본 30일 미사용 시 자동 삭제).
대화가 사라져도 `CLAUDE.md` + `WORK_LOG.md` + 이 파일 + git 커밋 메시지면 맥락은 복원된다.

## 작업 원칙 (사용자가 명시한 것)

- **코드 수정 전에 방향을 물어볼 것.** 조사·진단은 묻지 않고 진행해도 됨
- PSI 관련은 최소 변경
- 루트 `SOP_LATEST.html`을 고치면 **반드시** `cp SOP_LATEST.html public/SOP_LATEST.html`
- 검증은 Node vm 하네스로 (scratchpad의 `qtytest.mjs`, `psitest.mjs`, `arctest.mjs` 패턴)
