# 다음에 이어서 할 일

> 세션을 껐다 켜도 이 파일만 읽으면 바로 이어갈 수 있게 정리한 문서.
> 자세한 배경은 `WORK_LOG.md`(2026-08-12), 코드 구조·함정은 `CLAUDE.md` 참고.
> **작업을 끝내면 이 파일을 갱신할 것.**

## 지금 상태 (2026-08-18 기준)

- 운영 사이트: https://s-op-gungi.pages.dev — 코드·데이터 모두 최신 배포 완료 (커밋 `a4f8783`)
- 라이브 데이터: **26년 8월 파일 v1.4 (사용자가 2026-08-18 재작성한 판)**
  `기준일 8/1/26` · 원장 시트 `기초재고` · PSI 134행 · 판매계획 246품목 · Aging 총액 **78.66억**
  ⚠ **기준일이 8/13 → 8/1로, 총액이 80.43억 → 78.66억으로 바뀌었다.** 조회시점(현재고) → 월초 스냅샷(기초재고)
  으로 기준을 옮긴 것이지 데이터 사고가 아니다. 되돌리지 말 것 (아래 ⑱ 참고)
  ⚠ 2026-08-16에 판매계획 파서를 고쳤으므로 **그 이전 KV 데이터는 담당자 행이 섞인 옛 파싱본**이다. 되돌리지 말 것
  · 품목군 전월대비: **7/8/26 52.40억 → 8/1/26 78.66억 (+26.27억), "전월"** (2026-08-18 수정, 아래 ⑲)
- 배포 방법: 엑셀을 리포 루트에 넣고 `npm run deploy` (파일명 불필요, 최신 `.xlsx` 자동 탐색)
- 관리자 비밀번호: gitignore된 `.env.local`의 `ADMIN_PASSWORD`
  ⚠ **Codespace를 새로 만들면 이 파일이 없다.** 사용자에게 다시 만들어 달라고 할 것
  (VS Code 탐색기에서 `.env.local` 생성 → `ADMIN_PASSWORD=비밀번호` 한 줄)

## 방금 끝낸 것 ⓪ (2026-08-13) — PSI 표 0건일 때 원인 안내

"플랜트=횡성 + 다이소 전체 = 빈 화면" 문의에서 나온 작업. **버그가 아니라 기본 필터였다.**

- 원인: 3평판 필터 기본값이 `3평판 있는 품목만`인데 횡성 다이소 원본 14건은 3평판이 전부 0 → 전멸
- **횡성 코드(`9401xxx`)는 3평판·당월실적·판매계획·입고계획이 모두 0이고 재고만 있다.**
  다이소 품목의 2번째 생산처로 나중에 붙은 코드라 판매는 전부 향남 코드(`7302xxx`)로 나간다.
  그래서 필터를 풀어 보이게 해도 전 행이 회색 "산출불가"다 — 재고일수 분모가 없음
- 필터가 가리는 재고: 다이소 통합 0개 / 향남 0개 / 일반 PSI 8.8만개 / **횡성 79.4만개** (횡성만 이상치)
- 조치: 필터 로직을 `applyPsiFilters(값묶음)` 순수 함수로 분리하고, 0건일 때 필터를 하나씩 빼보며
  (leave-one-out) 살아나는 필터를 짚어 준다. **화면 기본값인 필터를 맨 위로 정렬** — 사용자가 직접 고른
  플랜트(해제 시 47건)가 위에 오면 정작 범인인 3평판이 묻힌다
- 사용자 선택을 코드가 자동으로 되돌리는 안(A안)은 뺐다. `CLAUDE.md`의 "UI 컨트롤을 프로그램으로 바꿔서
  사람마다 다른 화면" 함정과 같은 계열이고, 풀어도 회색 행만 나와서 실익도 없다
- 회귀검증: 필터 1,458개 조합에서 리팩터 전/후 결과 완전 일치 (scratchpad `emptystate.mjs`)

### 후속 — 플랜트별 분해로 이어짐 (아래 항목)

"통합으로도 보고 플랜트별로도 보고 싶다"는 요청으로 바로 이어졌다. 배분 규칙이 필요하다고 했던 건
3평판을 플랜트에 **나눠주려** 할 때 얘기였고, **분모를 통합으로 두면 나눌 게 없어져서** 해결됐다.

## 방금 끝낸 것 ⓪-2 (2026-08-13) — 다이소 플랜트별 분해

플랜트 필터를 고르면 다이소 통합 행이 **그 플랜트 몫**으로 바뀐다. 안 고르면 지금까지처럼 통합.

- 규칙과 기각한 대안은 `CLAUDE.md`의 **"다이소 플랜트 분해 규칙"** 에 정리 — 반드시 거기부터 읽을 것
- 핵심: `플랜트 재고일수 = 플랜트 기말재고 ÷ 통합 다음달 판매계획 × 30`.
  **플랜트별 재고일수 합 = 통합 재고일수**가 정확히 성립 (전건 검증)
- 신호등·3평판은 통합 기준(플랜트별로 걸면 향남 46일/횡성 103일이 각각 미달인데 합치면 149일로 정상)
- 진척율·3평판 기준 보조 열은 플랜트 분해에서 `—`. 입고 조정 input도 잠금
- 다이소 원본(개별 코드) 뷰도 같이 고침: 자체 계획이 0인 코드 25건이 소속 통합 수요를 분모로 빌려
  재고일수가 산출된다(전엔 전부 회색). **자체 3평판이 있는 코드는 덮어쓰지 않는다** — 0건 확인
- 검증 (scratchpad `plantview.mjs`, `final.mjs`):
  - 향남+횡성 = 통합 (기초·입고·판매·기말) 불일치 **0건**
  - 재고일수 가산성 불일치 **0건**
  - 플랜트 미선택 경로 216조합 회귀 불일치 **0건**
  - 통합 재고 3,265,035 = 향남 2,471,455 + 횡성 793,580 ✅
  - 금액 토글(수량/원가/매출) × 플랜트 조합에서 undefined/NaN 노출 없음

## 방금 끝낸 것 ① (2026-08-13) — ③탭 오차율 트렌드 차트 실측화 ✅ 배포됨

`renderHitTrend()`가 코드 상수 4개월(45.5/47.8/49.1/50.0)을 그리던 걸 실측으로 교체했다.

- 새 함수 `buildHitTrend()` — `hit.byMonth`를 `remapHitRowDaiso`+`aggregateHitRows` 거쳐 월별 WAPE 산출
- **파서의 `hitAccByMo`를 그냥 쓰지 않은 이유:** 그건 다이소 개별코드 raw 집계라 KPI 타일과 숫자가 어긋난다
  (전체 1월 53.8 vs 48.8, 6월 36.2 vs 26.5). 통합코드로 합치면 같은 품목 내 초과/미달이 상쇄되기 때문.
  화면의 KPI 타일·보정 리스트는 전부 remap+aggregate 경로라 **막대도 같은 경로로 맞췄다** — 월/채널 조합 5건 교차검증 일치 확인
- 판매계획이 빈 달(26/8의 7~12월)은 막대를 아예 안 그린다. 0%로 그리면 "오차 없음"으로 읽혀 정반대 의미가 됨
- 채널 필터(`fhc`)가 전 구간에 걸린다 (전엔 마지막 막대만 반응)
- y축 `max:100` → `suggestedMax:100` (4월 157.7%가 잘리던 문제)
- 라벨 `(MAPE)` → `(WAPE)` 오기 수정. 실제 산식은 처음부터 WAPE였다
- 업로드 데이터 없으면 `buildHitTrend()`가 null → 기존 상수 폴백 유지 (무회귀)
- 검증 하네스: scratchpad `trend.mjs`(월별/채널별 값), `trend2.mjs`(KPI 타일 교차검증 + 폴백)

## 방금 끝낸 것 ② (2026-08-13) — PSI 전 품목 현황 수량/원가/매출 토글 ✅ 배포됨

표 상단에 `수량 / 원가 / 매출` 버튼 3개(`setPsiUnitMode`). 기초·입고·판매·기말 4개 열만 백만원으로 환산하고
3평판·재고일수·신호등은 항상 수량·일수 기준(판정 결과 불변).

- 원가단가 3단 폴백(`getPsiPriceMap`): ① 현재고 재고금액÷재고수량 → ② 다이소 통합코드는 하위 `componentCodes` 재고 가중평균 → ③ 원가시트 배율 역산
  - **②가 핵심.** 통합 SKU(DAI-xx)는 현재고 시트에 그 코드가 없어 원가단가가 전부 0인데, 재고 대부분(326만개)이 여기 몰려 있다. ②를 빼면 원가 커버리지가 수량가중 24%로 떨어짐
- 매출단가는 기존 `getUnitPrice()` 그대로
- 26/8 파일 검증: 재고>0인 103 SKU 중 원가 103건(100%) / 매출 93건(90%, 수량가중 87%). 못 찾으면 `—`
- 전체 기초재고 = 수량 431만개 / 원가 73.0억 / 매출 278.4억
- 금액 모드에선 입고 조정 input이 읽기전용 텍스트로 바뀐다(금액→수량 역산 반올림 오차 방지). 정렬도 금액 기준으로 같이 전환됨
- 검증 하네스: scratchpad `pricecov*.mjs`(커버리지), `psiunit.mjs`(3모드 렌더), `spot.mjs`(환산값 손계산 대조)

## 방금 끝낸 것 ③ (2026-08-13) — PSI 전 품목 표에서 차월 이후 "3평판 기준" 열 제거 ✅ 배포됨

당월(8월)만 `진척율 기준` 보조 2열(기말재고·재고일수)을 유지하고, **차월 이후(9·10·11월)의 `3평판 기준`
보조 2열은 삭제**했다. 사용자 요청.

- **월 블록 폭이 달라졌다: 당월 7칸 / 이후 5칸** (전엔 전부 7칸). 총 컬럼 36 → 30
- 새 헬퍼 `psiMonthSpan(m)` / `psiMonthCellOffset(mi)` / `psiTotalCols()` (`PSI_MONTHS` 바로 아래)
  - ⚠ `recalcPsiRow`가 `bi=7+mi*7`로 셀 인덱스를 세고 있었다 — **이걸 안 고치면 입고 조정 후
    두 번째 달부터 엉뚱한 칸에 숫자가 박힌다.** 지금은 `psiMonthCellOffset(mi)` 사용
  - 보조 기준이 당월(진척율=실적 기반)만 남아 `recalcPsiRow`에서 보조 셀 갱신 자체가 불필요해짐
- `month-end` 구분선 클래스를 차월 이후엔 재고일수 열로 옮김(그 열이 월 블록의 마지막이 됨)
- 비고 산식 문구·플랜트 안내 문구에서 3평판 기준 설명 삭제
- 검증 (scratchpad `repro_psi_cols.mjs`): thead 총폭 30 = tbody 133행 전부 30칸,
  헤더 `3평판 기준` 0개 / `진척율 기준` 1개, 월별 오프셋(7/14/19/24)에서 기초·기말·재고일수 정위치 확인
- 배포 완료 (커밋 `daf9089`). 같이 커밋하며 `wrangler.toml` 끝에 실수로 들어가 있던 `ㅊ` 한 글자도 되돌렸다

## 방금 끝낸 것 ④ (2026-08-13) — ③탭 오차율 트렌드 차트 신설 + 금액/수량 토글

**차트가 화면에 없었다.** `buildHitTrend()`/`renderHitTrend()`는 있는데 `<canvas id="hitTrendChart">`가
git 히스토리 전체에 한 번도 없었다 — `renderHitTrend()`는 `renderHitKpis()`만 하고 null 캔버스에서 리턴.
①번 항목("트렌드 차트 실측화")은 **계산 함수만 만들고 화면엔 못 붙인 상태**였던 것. 이번에 캔버스를 신설했다.

- 위치: ③탭 KPI 타일 바로 아래 전체폭 (`.grid-5-5` 위), 헤더 우측에 `금액 / 수량` 버튼
- `hitTrendUnit` + `setHitTrendUnit` + `syncHitTrendUnitUI` (기존 `setInsightUnit`/`setHTUnit`과 같은 패턴)
- 수량 소스는 `hit.qty` — `hasPlan=false`면 버튼을 잠그고 "산출 불가" 안내를 띄운다.
  **선택 자체는 코드가 바꾸지 않는다** (`chAchieveMonth` 함정과 같은 계열). 버튼 상태 동기화는
  업로드 경로가 아니라 `renderHitTrend()` 안에 둬서 모든 열람자가 같은 화면을 본다
- 폴백 상수(`HIT_TREND_DEFAULT`)는 금액 기준이라 **수량 모드에선 쓰지 않는다** — 쓰면 가짜 숫자가 나온다
- 검증 (scratchpad `repro_hittrend.mjs`): 금액 1월 48.8~7월 36.5% / 수량 1월 36.6~7월 24.7%,
  채널 필터(다이소) 반영, `hasPlan=false` 잠금, 업로드 없음 시 금액 폴백 유지(무회귀) 전부 확인

## 방금 끝낸 것 ⑤ (2026-08-13) — PSI 표 폭 줄이기 (너비만)

가독성 개선 논의 중 사용자가 **"너비만"** 으로 범위를 한정했다. 나머지(sticky 고정열, 신호등 pill,
요약/상세 2단 뷰, 소계 행, 12월 추가)는 **논의만 하고 착수하지 않음** — 다시 꺼낼 때 아래 참고.

- 고정 6열 min-width 축소: 자재코드 86→72 / 품목명 210→178 / 품목군 72→54 / 구분 96→76 /
  플랜트 110→74 / 소진판단 74→60 (합 136px 절약)
- ⚠ **핵심은 `.psi-tbl.monthly{min-width:2700px}` → `2320px`.** 이걸 안 내리면 줄인 폭을
  월 열들이 나눠 가져서 **총 폭이 그대로**다. 열을 6개 지웠는데도 표가 안 좁아졌던 이유가 이것
- 구조 회귀 없음 확인 (thead 총폭 30 = tbody 133행 전부 30칸)

### 논의만 하고 보류한 것 (사용자 요청 시 재개)

- **월 5개월(12월까지)이 데이터 상한.** `sourceMonthlyPsi` 블록이 5개뿐이고,
  12월은 분모가 될 1월 판매계획이 시트에 없어 재고일수·신호등이 영구 회색
- 가독성 후보: ① 자재코드·품목명 `position:sticky;left:0` ② 재고일수를 신호등 색 pill로
  ③ 기초/입고/판매 흐리게 + 기말·일수 진하게 ④ 월 블록 교대 배경
  ⑤ 요약(월 2칸)/상세(월 헤더 클릭 시 5칸) 2단 뷰 → 5개월 넣어도 폭이 지금 절반
- **소계 행**: `thead` 맨 아래 sticky. ⚠ 재고일수는 평균 금지 — `Σ기말 ÷ Σ다음달계획 × 30`으로
  재계산해야 한다. 3평판은 다이소 통합/하위가 값을 공유해 필터 조합에 따라 이중계상 위험
- **횡성(9401xxx) 매출단가가 `—`인 원인 확인됨**: `getUnitPrice`가 ①SF 계획단가 ②PSI 계획금액÷수량
  두 경로뿐인데 횡성은 SF에도 PSI에도 자기 행이 없다(재고만 있고 판매는 향남 코드로 나감).
  원가는 현재고 재고금액÷수량이라 나온다. 고치려면 원가단가의 "통합 하위품목 차용"과 대칭으로
  **형제(향남) 코드의 매출단가를 빌려오는 폴백**을 붙이면 된다 (`daiso_code_map` 이미 있음)

## 방금 끝낸 것 ⑥ (2026-08-13) — PSI 신호등 필터에 기준월 추가 + 오차율 차트 목표선 제거

**신호등 필터가 당월치만 알고 있었다.** 필터는 파싱값 `d.sp` 하나만 봤는데 표에는 4개월치 신호등이
그려진다. 이제 `[신호등] [기준월]` 두 컨트롤로, 표에 그려지는 것과 같은 계산을 쓴다.

- 새 select `fsigmo`: 당월(8월) / 9월 / 10월 / 11월 / **아무 달이든**. 옵션 라벨은 `monthRealLabels`에서
  뽑으므로 엑셀이 롤되면 자동으로 따라간다. 기본값 `''`(당월)이라 안 건드리면 기존과 같은 화면
- `psiSigsByMonth(d)` / `psiSigMatches(d,want,moSel)` — `_renderPsiTbody`와 **똑같이** 첫 달부터
  `calcPsiMonth`를 굴리고 입고 조정값(`getPsiSimBuy`)도 태운다. 조정 후에도 필터=화면
- `PSI_FILTERS`에 등록해서 0건 진단·필터 초기화에도 자동으로 걸린다
- ⚠ **기존 동작이 4건 바뀐다.** `d.sp`에는 '기초재고 100개 미만이면 정상' 규칙이 있는데 표의 당월
  신호등(`calcPsiMonth`)에는 없어서 화면 🔴 / 필터 🟢로 어긋나 있었다. 화면 기준으로 통일 → 당월 위험 23→27건.
  넷 다 재고가 적어서 무시해도 되는 게 아니라 **기초재고 0~85인데 당월 판매계획이 66~83인 품절/품절임박** 건이었다
  (9311600, 9311606, 7000376, 7300768). 자잘한 품목은 기존 '기초재고 100개 이상' 필터로 거르면 된다
- 월별 위험 건수: 8월 27 / 9월 28 / 10월 28 / 11월 0(전부 산출불가) / **아무 달이든 34**
  → 당월엔 멀쩡한데 9~10월에 터지는 품목이 7건
- 오차율 트렌드 차트에서 **목표 50% 점선·좌측 라벨 제거**(사용자 요청). KPI 타일의 '목표 대비' 문구는 유지
- 검증 (scratchpad `repro_sigmonth.mjs`): 8개 조합 전부 화면 dot 기준 기대치와 일치

### 참고 — 3평판 필터에 하한을 걸면 안 되는 이유 (2026-08-13 확인)

7302377(합쎈 선물세트, 3평판 14)처럼 "전월에 한 번 나간 게 3으로 나뉜" 품목이 3평판 필터를 통과한다.
하한(예: 100개 이상)을 걸면 걸러지긴 하는데, **7302451(세븐일레븐 밀크씨슬)이 같이 사라진다** —
3평판 20인데 재고가 16,620개(약 70년치)로 재고 방치 관점에선 최악이다.
걸러야 할 건 3평판이 작은 품목이 아니라 재고·계획이 둘 다 없는 품목이고, 그건 기존 '기초재고 100개 이상' 필터의 몫.

## 방금 끝낸 것 ⑦ (2026-08-13) — PSI 표 소계 행 (헤더 바로 아래)

`thead`의 마지막 행(`<tr id="psiSubtotalRow">`)으로 넣어서 **sticky가 그대로 걸린다** — 133행을 스크롤해도
소계가 컬럼 밑에 붙어 있다. `renderPsiSubtotal()`이 `_psiState.data`(=필터 통과분)로 계산한다.

- 합계: 3평판 + 월별 기초·입고·판매·기말 (+당월 진척율 기말). 수량/원가/매출 토글을 그대로 따라간다
- ⚠ **재고일수는 평균이 아니다.** `Σ기말 ÷ Σ다음달계획 × 30`으로 재계산 — 행별 재고일수를 평균 내면
  재고 100개짜리와 30만개짜리가 같은 가중치가 된다. 셀 tooltip에 분자·분모를 그대로 적어 뒀다
- ⚠ **플랜트 분해·다이소 원본 뷰에서는 재고일수·3평판을 `—`로 비운다.** 여러 행이 같은 수요(분모)와
  3평판을 나눠 쓰기 때문에 더하면 중복 계상된다 (보조 열을 `—`로 비우는 것과 같은 이유)
- 금액 모드에서 단가를 못 찾은 행은 합계에서 빼고 라벨에 `· 단가없음 N건 제외`로 밝힌다
- `recalcPsiRow`에서도 다시 그린다 → 입고를 조정하면 소계가 같이 움직인다
- 검증 (scratchpad `repro_subtotal.mjs`): 5개 시나리오에서 소계행 폭 30 = 표 전체 폭,
  수량 모드 기말·재고일수가 독립 계산과 전건 일치, 횡성 뷰에서 `—` 정상 동작

## 방금 끝낸 것 ⑧ (2026-08-13) — 오차 분석에서 '계획 0' 행 제외

계획이 없는데 실적만 있는 행을 오차 분석에서 통째로 뺐다. `aggregateHitRows`의 마지막 필터
`plan>0||actual>0` → **`plan>0`** 한 줄.

- 이유: WAPE = `Σ|실적−계획| ÷ Σ계획`인데 이 행들은 **분자에만 들어가고 분모엔 안 들어가** 오차율을 부풀린다.
  26/8 파일 4월이 계획0 행 198건 때문에 157.7%로 튀어 있었다
- ⚠ **`aggregateHitRows`가 유일한 관문**이다. KPI 타일·트렌드 차트·보정 대상 리스트·오차 기여 TOP10이
  전부 여기를 거친다. 조건을 호출부에 복사하면 화면마다 숫자가 갈린다
- 반대쪽(계획은 있는데 실적 0, 7월 56건/8.8억)은 **그대로 뒀다** — 계획 2억인데 안 팔렸으면 100%가 맞는 값.
  사용자가 세 안(A 실적0제외 / B 계획0제외 / C 둘다) 중 B를 골랐다
- 화면에서도 아예 안 보이게(사용자 선택). 보정 대상 리스트 295→228행, 계획0 행 잔존 0건 확인
- 바뀐 값 (금액 기준 WAPE): 1월 48.8→44.3 / 3월 36.3→35.4 / **4월 157.7→118.4** / 6월 26.5→26.2 / 7월 36.5→35.3
  수량 기준도 같이 바뀜 (7월 24.7→23.5). 달성률 KPI도 분모·분자가 같이 줄어 7월 91.8%
- `verdict==='무계획 실적'` 분기는 이제 도달 불가지만 남겨 뒀다(필터를 되돌리면 다시 동작)

## 방금 끝낸 것 ⑨ (2026-08-13) — 표준원가를 **PSI 원가 환산에만** 반영

PSI 원가단가가 `현재고 재고금액 ÷ 수량`이었는데, **보류재고는 수량만 있고 금액이 0**(167라인·32.7만개)이라
91품목에서 단가가 심하게 희석됐다(와다다곰 21원, 향남 마그네슘 232원). 게다가 **횡성(9401) 코드는
SAP 표준원가 자체가 실제의 1.6~3.6배**(9401367 5,924원 vs 실제 1,633원)라 다이소 통합이 원가>매출로 뒤집혀 있었다.

**PSI 원가단가 우선순위: ① 단가 정정표(이관점검) → ② 결산 표준원가 → ③ 현재고 금액÷수량
→ ④ 다이소 통합은 하위품목 가중평균(단가는 하위품목의 표준원가) → ⑤ 매출단가÷배율 역산**

- `parseStdCostSheet` — 결산 파일 `자재수불 (26.7)` (`자재`/`표준원가`). 3,311개 자재
- `parsePriceOverrideSheet` — 이관점검 파일 `②PSI` (`품목코드`/`원가`/`실매출`). 20품목.
  ⚠ 두 시트 모두 **헤더를 값으로 찾는다** — 위쪽 빈 행 개수가 매달 다르고, ②PSI는 범위가 B2부터라
  `rows[i][0]`이 B열이다. 열 위치 하드코딩 금지
- 매출단가도 정정표의 `실매출`을 `getUnitPrice()`보다 우선 → DAI-7 원가 1,632 < 매출 1,996 **역전 해소**
- 결과: 횡성 9401367 1,633원 / 9401368 1,195원(정정표와 일치), 와다다곰 21→1,600원,
  다이소 통합 8월 기초 원가 49.6억 < 매출 58.5억

### ⚠ AGING(재고 Aging 리포트)은 손대지 않는다 — 사용자 결정

한 번 표준원가로 재평가해 봤다가 되돌렸다. **Aging 리포트는 현재고 시트 금액을 그대로 쓰는 게 업무 기준이다.**
(재평가하면 매트릭스 80.43→75.54억, 위험재고 1.86→1.99억으로 바뀌고 과거 추이와 단절된다.)
지금은 AGING·KPI·위험재고 전부 **기존 그대로 80.43억 / 1.86억**이다.
표준원가·정정표는 `getPsiPriceMap`에서만 쓰인다.

### 배포 파이프라인이 파일 3개를 각각 찾는다

**"가장 최근 xlsx 자동탐색"은 폐기했다** — 결산·점검 파일을 리포에 두면 엉뚱한 걸 월간 데이터로 파싱한다.
이름 패턴으로 가린다: `AGING_RE`(월간) / `STDCOST_RE`(결산) / `OVERRIDE_RE`(이관점검).
`--std=`, `--override=`로 직접 지정도 가능. 결산·정정표가 없으면 기존 방식으로 폴백(무회귀).
**매달 결산 파일을 새로 받아야 한다** — 없으면 지난달 원가로 이번 달 PSI 금액을 환산한다.

## 방금 끝낸 것 ⑩ (2026-08-13) — PSI 표 열 너비 고정

수량/원가/매출 토글을 누를 때마다 값의 자릿수가 달라져 열 폭이 흔들리던 것을 고정했다.
헤더 leaf 셀에 `psi-col`(78px) / `psi-col-buy`(100px, input+초기화 버튼) 클래스를 붙였다.
78px은 **소계 행의 최댓값(4,310,275)** 기준 — auto 레이아웃이라 내용이 폭을 넘으면 그 열만 늘어나므로,
어느 모드에서도 안 넘게 잡아야 흔들리지 않는다.

## 방금 끝낸 것 ⑪ (2026-08-13) — ①탭 위험재고 차트 전체폭 이동 + 막대 합계 라벨 반올림 오차 수정

**"80.5억이 왜 나오냐"에서 시작했다.** 총 재고금액은 80.4292억(KPI 카드·매트릭스는 80.4억)인데
[전체 재고 Aging 구성] 막대 위 합계만 80.5억이었다.

- 원인: 데이터셋을 만들 때 **구간별로 먼저 `Math.round(v*10)/10`** 하고, `stackedTotalLabelPlugin`이
  **그 잘린 값들을 다시 더해서** 라벨을 그렸다. 8월 = 55.1+17.9+5.6+1.3+0.6 = 80.5 (원값 80.4292)
- ⚠ 8월만이 아니라 전 구간 문제였다: 1월 48.3(48.4) / 5월 51.3(51.4) / 7월 52.3(52.4)
- 조치: 데이터셋에 `rawData`(반올림 전) 배열을 같이 싣고, 플러그인이 **위치는 `data`(막대 끝), 숫자는 `rawData`** 로
  나눠 쓴다. `rawData`가 없는 데이터셋은 기존대로 `data`를 쓴다(무회귀). 붙인 곳 4군데 —
  `renderTrendChart`, `renderChAgingChart`의 stacked/grouped/snap
- 검증 (scratchpad `verify_trend.mjs`): 8개월 전부 라벨 = `trend_data.계`와 일치 (8월 80.4억원)

### 같은 패널 레이아웃 변경 (사용자 요청)

`3fr : 2fr` 좌우 배치에서 위험재고 차트가 좁아 월이 늘수록 포인트 라벨이 겹쳤다. **매달 한 칸씩 느는 구조**라
근본적으로 위아래 전체폭으로 쌓았다(Aging 구성 240px 위 / 위험재고 210px 아래).

- 캔버스에 직접 그리던 좌측 **`목표 1억` 글자와 `layout.padding.left:92` 제거.** 이 92px이 좁은 칸에서
  그림 폭을 크게 깎고 있었다. 목표는 점선 + 범례(`목표 (1.0억원 이하)`)가 이미 알려준다.
  타이틀 `[위험재고 합계 vs 목표 (1억원)]`과 KPI 카드 ②의 목표 1.0억은 그대로 유지
- 포인트 라벨 `1.9억원` → `1.9억`(`fmtAmt(v,'억')`), `padding.top:18`로 최상단 라벨 잘림 방지
- `riskData`도 미리 반올림하지 않는다 — KPI 카드 ②와 같은 원값을 `fmtAmt`에 넘겨야 숫자가 안 갈린다

## 방금 끝낸 것 ⑫ (2026-08-13) — PSI 표: 재고가 아예 없는 품목은 '품절!' 대신 '재고없음'

**`품절!`은 두 가지가 섞여 있었다.** 팔려서 떨어진 것과, 애초에 굴릴 재고가 없는 것.
후자만 회색 `재고없음`으로 뺐다(사용자 요청).

- 판정: **기초재고 0 + 전 기간 입고계획 0인 행**의, **그 달 기초·입고가 둘 다 0 이하인 칸**.
  26/8 기준 3개 SKU / 8칸 — 9311606 직영몰 쏘팔메토 · 7000376 덴티가드 시린이 · 7300768 덴티가드 칫솔
- 팔려서 떨어진 10개 SKU(7300120 기초 6,729→7월 −961 등)는 `품절!` 그대로
- 새 함수 `psiRowNeverStocked(d)` / `psiNoStockCell(d,begin,buy,days)` — **`calcPsiMonth` 안에서만 호출한다.**
  신호등 필터(`psiSigsByMonth`)도 같은 함수를 거치므로 표와 필터가 자동으로 일치한다. 호출부에 복사하면 갈린다
- 신호등도 회색으로 내렸다 → **월별 위험 건수 8월 27→24 / 9월 28→25 / 10월 28→26 / 아무달 34→31**
  (⑥에서 일부러 드러냈던 4건 중 3건이 다시 빠진 것. 남은 9311600은 기초재고 85로 실재고가 있어 유지)
- 입고를 직접 조정해 넣으면 그 달부터 판정에서 자동으로 빠진다(`begin>0||buy>0`이면 통과)
- 플랜트 분해·다이소 하위 뷰(`d.sigByMonth` 있는 행)는 제외 — 신호등을 통합 기준으로 판정하는 통로라 건드리지 않는다
- ⓪탭 재고일수 TOP12는 이미 `d.inv>=100` 필터가 있어 영향 없음
- 검증 (scratchpad `soldout.mjs`, `verify_nostock.mjs`): 3행만 회색 전환, 나머지 10행 품절 유지,
  **표 기준 red 건수 = 필터 기준 red 건수** 전 월 일치

## 방금 끝낸 것 ⑬ (2026-08-14) — v1.4 반영 + 차월 권장계획 월 정렬 수정

사용자가 **26년 사업계획을 고쳐서 v1.4**(`260813 v1.4.xlsx`)를 올렸다. 반영하면서 오래된 코드 버그를 하나 잡았다.

### 데이터 쪽 변화 (v1.1 → v1.4)

- **사업계획 목표의 한 칸 밀림이 시트에서 해소됐다.** 7월이 0이고 12월이 없던 게 제자리를 찾아
  `50.5/52.1/63.7/68.4/71.4/68.4/63.5/68.2/83.8/69.2/71.8/69.0` = **연간 정확히 800.0억**(전 731.0억)
- **PSI 12월 판매계획이 채워졌다 → 11월이 살아났다.** 133건 전부 회색이던 게 위험 24 / 정상 60 / 산출불가 47
- 입고가 늘고 계획이 조정되며 위험 신호 감소: 8월 24→20 / 9월 25→15 / 10월 26→14
- PSI 133→134행 (`7400187 합쎈 키즈 30포` 신규, 재고 0·3평판 0)
- **안 바뀐 것**: 현재고·현황 시트 그대로라 총 재고금액 80.429억 / 위험재고 1.862억 동일.
  오차율분석 시트도 그대로라 **4월 WAPE 118.4%와 2303xxx 코드 문제는 남아 있다**(아래 미해결 이슈)

### 코드 수정 — `getNextPlan`이 두 달 뒤 계획을 집어 왔다

`idx=nextMonth-6` 절대 오프셋이 원인. SF 계획 블록(AG~AK)은 **매달 롤링**한다 —
26/8 파일은 8~12월, 26/7 파일은 7~11월. 26/8에서 차월 8월을 물으면 index 2(=10월)를 집었다.

- `parseSalesPlanSheet`가 계획칸 라벨을 **시트 헤더(35행 AG~AK)에서 읽은 실제 년월**로 붙인다
  (`planMonthMeta`). 헤더를 못 읽으면 옛 `6+i`로 폴백 — 무회귀
- 새 함수 `psiPlanIdxForMonth(nextMonth)` — 라벨 배열에서 `indexOf`로 자리를 찾는다.
  **⚠ 절대 오프셋 산식으로 되돌리지 말 것.** 매달 롤링해서 반드시 다시 어긋난다
- `nextMonth<6||nextMonth>10` 하드코딩 가드도 제거(`idx<0`으로 대체) — 11·12월이 임의로 잘리던 것도 같이 풀림
- ⚠ **`planQty[i]`를 위치로 쓰는 다른 두 곳은 안 건드렸다** (`renderSkuDetailModal`, 소진 판단 —
  PSI 월키 i번째와 위치가 같음을 250셀 대조로 확인). 배열 순서는 그대로 두고 `month` 라벨만 고쳤다
- 검증 (scratchpad `verify_nextplan.mjs`): 세 파일 전부 불일치 0건
  - v1.4·v1.1(8월 파일) 계획칸 8~12월 → 차월 8월 = **index 0** ✅
  - 26/7 파일 계획칸 7~11월 → 차월 8월 = **index 1** ✅ (옛 산식은 여기서도 2를 집어 9월을 봤다)

### 📌 지난 기록 정정 — "두 오류가 상쇄된다"는 판단은 틀렸다

`NEXT.md` 미해결 이슈 #1에 "SF 시트 값이 한 달 밀렸고 코드도 밀려서 우연히 맞는다"고 적어 뒀었다.
**PSI 판매계획과 250개 셀을 대조한 결과 시트는 처음부터 헤더와 일치했다**(offset 0에서 245/250).
밀린 건 SKU별 계획이 아니라 **요약의 사업계획 목표 행 하나뿐**이었고, 그건 v1.4에서 사용자가 고쳤다.
즉 상쇄되는 오류는 없었고 코드만 어긋나 있었다.

## 방금 끝낸 것 ⑭ (2026-08-14) — ①탭 품목군별 전월대비 재고 증감 표 ✅ 배포됨

"7월 52.4억 대비 어떤 품목군이 늘었나"에서 나온 작업. 재고Aging리포트 카드 **바로 아래** 새 카드.

### 숫자 (26/7 → 26/8, 총 +28.03억)

| 품목군 | 7/8 | 8/13 | 증감 | 기여율 |
|---|---:|---:|---:|---:|
| **닥터베어** | 23.88 | 44.86 | **+20.98** | **74.9%** |
| 집중샷 | 5.57 | 9.84 | +4.26 | 15.2% |
| 퍼펙트샷 | 3.16 | 5.14 | +1.97 | 7.0% |
| 합쎈 | 6.28 | 7.68 | +1.40 | 5.0% |
| 에너씨슬 | 3.77 | 3.85 | +0.08 | 0.3% |
| 플래티넘 | 6.34 | 5.80 | −0.55 | — |

증가분의 3/4이 닥터베어(다이소). SKU로는 9401368 마그네슘 +6.31 / 9401367 칼마디 +4.76 /
9401385 밀크씨슬 +3.55 순 — **전부 횡성(9401) 코드**다.

### 구조

- 규칙·함정은 `CLAUDE.md`의 **"품목군 전월대비"** 에 정리 — 손대기 전에 거기부터 읽을 것
- 품목군은 `현재고`에 없고 `기준정보` 시트에 있다(`parsePkgGroupMap`). 자재코드/품목군 열 쌍이 두 블록 반복 + 범위가 A2부터
- 집계는 `parseInventory(ws,pkgMap)` 안에서 원본 `amt`로 — 매트릭스 `sku.a`(0.1백만원 반올림)를 다시 더하면 총액이 어긋난다
- 전월 소스 3단: `--prev=` → 리포의 한 달 앞선 Aging 파일 → **라이브 KV의 직전 달 스냅샷**.
  ③ 덕분에 다음 달부터는 옛 엑셀을 안 들고 있어도 자동으로 이어진다
- 카드는 `pkg_prev`가 있을 때만 보인다. 웹에서 파일 하나만 올리면 안 보임(무회귀)
- 금액/수량 토글, 품목군 행 클릭 → 하위 SKU 증감 내림차순 펼침
- 데이터 증가 gzip +4.3KB (249.4KB)

### 검증 (scratchpad `verify_pkgdelta.mjs`, `verify_pkgrender.mjs`)

- 합계 52.3973 / 80.4292억 = 화면 트렌드값과 일치, 미분류 0건, 전 SKU 합 = 품목군 합 = 총액
- 파일 선택이 8월 v1.1이 아니라 7월 v5.2를 집음
- **방향 가드**: 7월 파일에 8월 스냅샷을 전월로 넘기면 버린다(증감 부호 뒤집힘 방지)
- 무회귀: 전월 없이 파싱 시 KPI 총액·PSI 134행·Aging 매트릭스 JSON 완전 동일
- `npm run deploy -- --dry-run` 전 구간 통과

### 배포 (커밋 `fa0aef0`)

데이터 265KB(gzip) 반영 + 코드 push 완료. 라이브 HTML md5 = 로컬 `public/SOP_LATEST.html` 일치 확인.
라이브 KV `pkg_snapshot` = 2026.08 / 8/13/26 / 80.43억 / 품목군 9개 — **다음 달엔 이게 자동으로 전월이 된다.**

## 방금 끝낸 것 ⑮ (2026-08-14) — PSI 기초재고에서 보류재고 제외

사용자가 이전 세션에 요청했는데 **반영이 안 돼 있었다**(커밋·코드 어디에도 흔적 없음). 이번에 처리.

### 왜 섞여 있었나

```
PSI!BQ8 (전체재고 수량) = SUMIFS(기초재고!$AS:$AS, 기초재고!$B:$B, 자재코드)
기초재고!AS = 가용 + 품질검사 + 보류      ← 494행 전건 확인, 예외 0건
```

기초재고 = `BQ − BS(6개월↓)`인데 BQ에 보류가 들어 있어 그대로 남아 있었다.
BQ를 SUMIFS로 재현하니 169행 전건 일치 — 시트 수식이 근거다.

### 조치

- 새 함수 `parseHoldStock(ws)` — `기초재고` 시트 N열(보류). **PSI!BQ가 참조하는 바로 그 시트**에서 읽는다
- ⚠ **이중차감 방지**: `BS = BZ+CA` = AO가 `유효기간 초과`/`6개월 ↓`인 것의 합이라 그 구간 보류는 이미 빠졌다.
  `parseHoldStock`이 그 두 구간을 세지 않는다 (안 하면 26/8 기준 5개 중복)
- **품질검사(30만개)는 남겼다** — 공정 통과 후 판매 가능. 사용자가 세 안 중 '보류만 제외'를 선택
- `parsePSISheet(ws,holdByMat)` 한 곳만 고치면 나머지는 따라온다:
  표는 `calcPsiMonth(d,m,prevEnd)`가 월을 체인으로 계산하고, 다이소 통합은 하위코드 합산이라 자동 전파
- 행에 `invRaw`(제외 전)·`hold`를 같이 실어 뒀다. 안내 문구 3곳(alert 2 + 비고 산식 1) 갱신

### 바뀐 숫자

| | 변경 전(보류 포함) | 변경 후 |
|---|---:|---:|
| PSI 기초재고 합계 | 4,310,275개 | **3,126,910개** (−1,183,365 / −27.5%) |
| 8월 위험 | 20건 | 23건 |
| 9월 위험 | 15건 | 19건 |
| 10월 위험 | 14건 | 19건 |
| 11월 위험 | 24건 | **38건** |

값이 바뀐 품목 98건, 기초재고가 0이 되는 품목은 0건. 큰 건은 전부 다이소 향남(7302xxx) —
7302230 마그네슘 137,316→32,916 / 7302240 오메가3 148,848→70,680 / 7302236 밀크씨슬 46,428→**12**.
보류의 94%가 `18개월↑` 구간이라 이미 빠지는 구간과 거의 안 겹친다.

**Aging 리포트·품목군 표는 그대로 80.4292억** — 가용+품질+보류 합산이 업무 기준이다(2026-08-13 결정).

### 검증 (scratchpad `hold.mjs`, `hold2.mjs`, `verify_hold.mjs`)

- 엑셀 직접 계산과 94행 전건 일치, `inv ≤ invRaw`·음수 0건
- 다이소 통합 40건 = 하위코드 합 (불일치 0) — 보류 제외가 통합까지 전파됨
- 라이브 KV(변경 전)와 같은 함수로 before/after 대조해 위 표를 산출
- 품목군 전월대비 표 회귀 없음

## 방금 끝낸 것 ⑯ (2026-08-16) — 판매계획 시트를 행/열 위치가 아니라 값으로 읽게 ✅ 배포됨

**"순매출 달성율·채널 달성율이 이상하다"** 문의에서 시작. 원인은 파서가 채널 블록 끝을 못 잡은 것.

- **26/8 담당자 사고**: SKU 상세 블록 머리글이 26/7엔 2줄(34행=컬럼제목 / 35행=년월)이었는데
  26/8에 35행 한 줄로 합쳐졌다. `담당자`(H열 컬럼 제목)가 년월 숫자 행과 같은 줄이 되었고,
  `readSection`이 `총계`에서 안 멈춰 **채널로 주워담아** 월마다 `2026.08`(=2,026억)이 더해졌다.
  → 채널 합계 2,086억 / 달성율 4,131%, 브랜드 순매출은 배율 역산 탓에 28배(1,735억)
  → 26/7 파일이 멀쩡했던 건 그 자리가 `"판매실적"` 같은 **글자**라 0으로 걸러졌을 뿐 — 운이었다
- 고정 위치를 전부 제거: `rows[34]` / `rows.slice(35)` / `ACTUAL_START_COL=20` / `planCols=[32..36]` /
  `row[8+i]` / `rows.slice(0,35)` → `findSalesPlanSkuHeader`·`splitSalesPlanHeaderBlocks`로 값에서 찾는다
- **순매출을 시트 29행 `순매출(신제품제외)`에 맞췄다.** 예전 `다이소 ×1.0 / 나머지 ×0.83` 하드코딩은
  실제 할인율이 8~18%로 움직여 26/6에 4.7억 어긋났다. 이제 시트와 **오차 0.0000**
- 조용히 폴백하지 않게 업로드 경고 추가 (헤더 미인식 / 순매출 행 없음 / **브랜드 합계 ≠ 채널 합계**)
- 검증: 엑셀 3개 + 밀림 시나리오 4종(2행/3행/5행 삽입, 계획 5→6개월)에서 결과 동일.
  수정 전 코드로 같은 시나리오를 돌리면 실적월이 `[1~5]`로 조용히 폴백하고 24개월 라벨이 전멸했다
- 상세는 `CLAUDE.md`의 `parseSalesPlanSheet` 절 참고

## 방금 끝낸 것 ⑰ (2026-08-16) — ③탭 채널별 오차율 표 + 오차 금액 차트 ✅ 배포됨

월별 트렌드 차트 밑에 신설. 함수: `buildChannelWape`·`renderChannelWape`·`renderChWapeGap`·`openChDrill`.

- **왼쪽 = 오차율(%) 표 / 오른쪽 = 오차 금액 발산 막대.** 둘의 순위가 정반대라 서로를 보완한다:
  26/8 7월 기준 제로샵 107%지만 0.58억(2.1%), 직영몰 37%지만 10.26억(37.9%)
- 오차 금액 막대는 **초과(+, 초록) / 미달(−, 빨강)** 로 갈랐다. WAPE는 절대값이라 방향이 없다.
  다이소 −4.8억 미달 / 쿠팡 +2.7억 초과 — 오차율은 26%·48%로 비슷한데 대응은 반대
- 숫자 칸 클릭 → `openChDrill` 팝업(그 채널·그 달 품목을 |차이| 큰 순, 오차 기여도 열 포함)
- 색 임계값은 `wapeClass()` 한 곳으로 모았다 (보정 대상 리스트 총계 배지와 같은 30/50%)
- 채널 필터(`#fhc`)에 6개만 하드코딩돼 있어 오프라인기타·편의점·벤더·제로샵·복지몰·온라인기타를
  못 골랐다 → `syncHitChannelFilterOptions`로 데이터에서 채운다.
  ⚠ 원본이 아니라 `aggregateHitRows`를 통과시켜야 한다(계획 0인 `동남아`가 목록에만 남는다)
- **폐기한 시도 2개** (다시 하지 말 것):
  ① 셀 배경 음영(초록/노랑/빨강) → "음영 빼고 가독성 좋게" 지시로 제거
  ② 채널별 꺾은선 차트 → 4월 이상치(쿠팡 178%)가 Y축을 0~180%로 끌어올려
     나머지 6개월(16~94%)이 플롯의 9~52%에 눌렸다. 270px에 5개 라인이 86px 안에 겹침
- 표는 `table-layout:fixed` + colgroup으로 폭 고정(채널 132 / 월 68 / 계획 92). `width:100%`면
  화면 전체로 늘어나 숫자가 흩어진다

## 방금 끝낸 것 ⑱ (2026-08-18) — 재고 Aging 원장을 `현재고` → `기초재고`(월초)로 전환 ✅ 배포됨

사용자가 엑셀 쪽을 먼저 정비하고 "파일 업데이트했는데 확인해달라"고 해서 검증 → 배포까지 한 건.

- **왜 바꿨나**: 같은 파일의 `현황` 시트가 8월 블록 SUMIFS를 `현재고!` → `기초재고!`로 옮겼다.
  원장만 현재고로 두면 ①탭 **KPI 카드(현황 기준)와 매트릭스(원장 기준)가 한 화면에서 갈린다.**
- `handleUpload`: `findSheetName(wb,'기초재고')||findSheetName(wb,'현재고')` — 기초재고 없는 옛 파일은 폴백(무회귀)
- 읽은 시트명을 `result.inventory_sheet`에 남겨 헤더 '데이터 기준: ○○ AM2'에 그대로 표시
- **채널 공백 누락 경고 신설**: 채널이 비어 통째로 빠지는 금액을 상태줄에 ⚠로 노출.
  단 기초재고 시트엔 타사업부 원장이 섞일 수 있어 **기준정보(품목군 맵)에 등록된 건기식 자재일 때만** 사고로 센다
- **잠재 버그 하나 같이 잡음**: `parseHitSheet._isSummaryRow`가 정의되지 않은 `chCol`을 참조.
  `mat`·`name`이 둘 다 빈 완전 공백 행이 와야 터지는 지연 폭탄이었다 → `pickHitChannel(row)`으로 교체
- `renderPkgDelta`: 두 스냅샷 간격이 1개월이 아니면 머리말을 `N개월 전`으로 (숫자·계산은 그대로)

### 검증 (3중 교차 일치, 소수점까지)
- 시트 컨트롤 총계 `기초재고!AT1` = 7,866,427,939원
- 파서 매트릭스/KPI 합계 = **78.66427939억** (수량 4,355,274개 = 시트 I+K+N 전체합과 동일)
- `현황` 시트 8월 트렌드 계 = **78.66427939억**
- 채널 공백 **0행 / 0억** (직전 파일에서 지적했던 `(채널)유통채널` 수식 미채움 전부 해소)
- 7,635행 중 실데이터 503행, 나머지 7,130행은 수식 패딩 공백행 — 버려진 데이터 없음
- 판매계획 `__layout.ok=true`, 채널 합계 정상 스케일 (26/8 담당자 사고 재발 없음)
- 배포 후 라이브 HTML md5 = `public/SOP_LATEST.html`과 일치 확인

### ✅ 해결됨 (2026-08-18) — 품목군 전월대비를 7월(52.40억) 기준으로 되돌림

**위 항목("2개월 전으로 뜬다")은 끝났다.** 아래 "방금 끝낸 것 ⑲" 참고.
전월 = `7/8/26 52.40억` → 당월 `8/1/26 78.66억` (**+26.27억**), 머리말 **"전월"**.
`52.40억`은 `현황` 시트 트렌드의 7월 값이자 **7월 회의자료에 나간 숫자**다.
7/1 기초재고가 든 파일을 따로 받을 필요는 없어졌다.

## 방금 끝낸 것 ⑲ (2026-08-18) — 품목군 전월대비 전월 원장 시트 자동 선택 ✅ 배포됨

사용자: *"품목군별 전월대비 재고 증감 7월 대비로는 안 되니"* → *"7월 건기식 aging해서 회의자료가 있잖아"*

**원인.** 8/18에 원장을 `현재고 → 기초재고`로 옮길 때 **전월 소스도 같이 밀려갔다.**
`readPrevPkgSnapshot`이 26/7 파일에서도 `기초재고`를 집는데, 그 파일의 기초재고는 **6/1 스냅샷**이다.
→ `6/1 62.20억 → 8/1 78.66억`, 머리말 "2개월 전".

**62.20억은 어디에도 없는 숫자였다.** 같은 8월 파일 `현황` 시트 트렌드는
`1월 48.41 / 5월 51.39 / 6월 61.00 / **7월 52.40** / 8월 78.66`이고, ①탭 트렌드 차트가 이미 그걸 그리고 있었다.
**같은 화면 안에서 7월 값이 52.40 vs 62.20으로 갈려 있었다.** 52.40억이 7월 회의자료에 나간 숫자다.

**조치.** `readPrevPkgSnapshot(prevPath, base, curYearMonth)` — 전월 파일 안의 원장 시트 중
**당월보다 앞서면서 기준월이 가장 최근인 것**을 고른다. 동률이면 `기초재고`.
파일명이 아니라 **시트 AM2(`parseBaseYearMonth`)** 로 판단하므로 파일이 계속 쌓여도 자동으로 맞는다.

| 전월 파일 | 후보 | 선택 |
|---|---|---|
| 26/7 v5.2 | 기초재고 2026.06 / 현재고 2026.07 | **현재고** (7/8, 52.40억) |
| 26/8 v1.4 (9월 배포 때) | 기초재고 2026.08 / 현재고 2026.08 | **기초재고** (8/1, 78.66억) |

- 손댄 파일: `tools/lib/publish.mjs`(`readPrevPkgSnapshot` 재작성, `agingYearMonth` 신설), `tools/deploy.mjs`(당월 기준월 전달 + 선택 시트 로그)
- **`SOP_LATEST.html`은 안 건드렸다** — 배포 도구만 바뀌었으므로 코드 배포도 필요 없다
- 검증 (라이브 KV 되읽기): 전월 `7/8/26 2026.07 52.40억 (시트 현재고)` / 당월 `8/1/26 2026.08 78.66억` /
  머리말 `전월` / **현황 트렌드 7월 52.40억과 일치** / PSI 134행·기준일 8/1/26 무회귀
- 미래 케이스 시뮬레이션(`curYm='2026.09'`)에서 26/8 파일 → `기초재고` 선택 확인
- ⚠ **검증 기준선을 기억할 것: 전월 카드 합계 = `현황` 트렌드의 그 달 값.** 어긋나면 원장 시트를 잘못 고른 것이다.

### 조사하다 나온 별건 — 원장 금액이 비어 있는 자재 (미조치)

기준 대조 중 발견. `(26년 7월) 재고자산 결산` 파일의 `자재수불 (26.7)` 기말(7/31)과
Aging 8/1을 자재 단위로 맞대보면 **금액 합계는 −0.52억(0.7%)으로 거의 일치**하는데, 개별로 보면:

- `9401386 다이소 닥터베어 멀티비타민 미네랄` 85,320개가 **원장 금액 0** (수불 1.67억)
- `7301428 와다다곰 리유저블백` 0.22억 / `7301427 돗자리` 0.20억도 같은 증상
- 합쳐서 **①탭 총액 78.66억이 약 2.1억 과소 계상**

또 기준정보에 건기식으로 등록됐는데 원장에 채널이 안 붙어 `skippedNoChannel`로 빠지는 자재가 **25개 0.66억**.
**수량은 두 시트의 정의가 아예 다르다** (Aging에 금액 0짜리 재고가 대량 포함, 116개 중 44개 자재 112.7만개 차이)
— 자재수불을 수량 비교에 쓰면 안 된다.

→ 사용자에게 보고만 한 상태. 원장 확인이 필요한 사안이라 코드로 손대지 않았다.

## 방금 끝낸 것 ⑳ (2026-08-18) — ①탭 플랜트 필터 + 자재유형 필터 정상화 ✅ 배포됨

사용자: *"재고Aging리포트에서 플랜트 추가 가능하니"* → *"필터로"*

- **플랜트 필터 신설.** 원장 D열(플랜트)/P열(플랜트명) → `sku.p` + `result.inventory_plants`.
  드롭다운은 데이터에서 그린다(코드에 목록을 안 박음 — 생산처가 늘어도 무수정).
  26/8: **향남 68.87 + 횡성 9.79 = 78.66억**, 전체와 정확히 일치.
- **자재유형 필터가 이미 깨져 있었다** — 옵션은 `(DW)상품`인데 26/8 파일 값은 `상품`이라
  **고르면 빈 표**가 나왔다. 옵션을 데이터 기반으로 바꿔 같이 고쳤다.
  (상품 65.0 + 완제품 13.5 + 포장재 0.2 = 78.7억)
- **드릴다운이 필터를 무시하던 것도 고쳤다.** `matrixSkuFilter()`/`filterMatrixCell()`을
  `renderMatrix`와 공유. 횡성 선택 시 드릴다운 SKU가 전부 `9401` 코드로 나오는 것 확인.
- 필터 시 해당 없는 채널 행 제거 (횡성은 `건기식(다이소) 18개월 이상` 한 칸뿐).
- 필터가 걸리면 표 아래에 **"KPI 카드·재고 추이 차트는 현황 시트 기준이라 전체 78.66억 그대로"** 안내.
  ⚠ 카드를 필터에 맞춰 바꾸려 들지 말 것 — 원장 기준과 현황 기준이 섞인다.
- 검증: 플랜트×자재유형 12조합 교차 합산 일치 / 필터 해제 시 렌더 HTML 완전 동일 /
  라이브 되읽기 `sku.p` 누락 0건 · 향남 68.87 · 횡성 9.80 · 라이브 HTML = 로컬 일치
- 상세는 `CLAUDE.md`의 **"①탭 매트릭스 필터"** 절

## 방금 끝낸 것 ㉑ (2026-08-18) — ①탭 필터를 창고 → 생산처(품목 기준)로 ✅ 배포됨

사용자: *"횡성공장 때문에 재고금액이 얼마나 상승했나"* → *"행말고 횡성품목 기준으로 봐야할 것 같은데,
횡성품목이 향남창고에도 있고 횡성창고에도 재고가 있단 말이지"* → *"aging에서 플랜트는 품목 기준으로"*

- 앞서 붙인 **창고(원장 플랜트) 필터를 생산처(품목 기준)로 교체**했다. 창고 기준은 횡성을 과소 계상한다.
  | | 향남 | 횡성 |
  |---|---:|---:|
  | 창고(원장 플랜트) | 68.87억 | 9.80억 |
  | **생산처(품목)** | **65.23억** | **13.43억** |
  차이 **3.63억** = 횡성 생산 품목인데 향남 창고에 보관 중인 재고
- 출처는 `다이소 마스터` 시트의 `플랜트` 열. `parseDaisoMasterSheet().plantByMaterial` 재사용.
  비다이소 품목은 원장 플랜트명으로 폴백(전부 향남 창고). `sku.sp` / `result.inventory_sources`.
- `sku.p`(창고)는 데이터에 남겼다 — 창고 필터가 다시 필요해지면 바로 붙일 수 있다.
- 검증: 향남 65.23 + 횡성 13.43 = 78.67억(전체 일치) · `sp` 없는 sku 0건 ·
  생산처×자재유형 교차 정합 · 필터 해제 시 렌더 HTML 완전 동일 ·
  횡성 드릴다운 SKU 8종 전부 9401 코드(향남 창고 보관분 포함) · 라이브 되읽기 일치

### 이 작업 중 확인한 업무 숫자 (화면에는 아직 없음)

**생산처 기준 전월대비** — 7월 파일의 채널 미부착분까지 되살려 계산한 값:

| 생산처 | 7월 Aging | 7월 누락(채널 미부착) | 7월 실제 | 8월 | Δ실제 |
|---|---:|---:|---:|---:|---:|
| **횡성 품목** | 0 | 6.38 | **6.38억** | **13.43억** | **+7.05억** |
| 향남 품목(다이소) | 22.65 | 0 | 22.65억 | 19.82억 | −2.83억 |
| 비다이소 | 29.75 | 3.45 | 33.20억 | 45.41억 | +12.21억 |
| 합계 | 52.40 | 9.83 | 62.23억 | 78.66억 | +16.44억 |

→ **횡성 때문에 오른 금액은 +7.05억.** 화면상 "+13.43억"으로 보일 수 있는데
그 중 6.38억은 7월에 채널 미부착으로 안 잡혀 있던 게 이번 달 잡힌 것이다.
⚠ 양쪽 모두 **8월 마스터 기준**으로 생산처를 통일해 계산했다(26/7→26/8에 22개 코드의 지정이 바뀌었다).

## 방금 끝낸 것 ㉒ (2026-08-18) — 26/7 파일 채널 수식 수정 반영 ✅ 배포됨

사용자가 담당자 확인 후 **26/7 파일의 `현재고` 시트 AV열 수식을 414행까지 채워** 다시 올렸다.

- 채널 빈칸 **42행 9.83억 → 0행 0억**. 7/8 총액 **52.40억 → 62.23억**
- 품목군 전월대비: **7월 62.23억 → 8월 78.66억 (+16.44억)** ⟵ 예전 +26.27억은 누락분이 섞인 값이었다
- 생산처 기준 증감이 이제 실제와 맞는다:

| 생산처 | 7월 | 8월 | 증감 |
|---|---:|---:|---:|
| 향남 | 55.85억 | 65.24억 | +9.39억 |
| **횡성** | **6.38억** | **13.43억** | **+7.05억** |
| 합계 | 62.23억 | 78.66억 | +16.44억 |

- ⚠ **부수 사고를 하나 막았다**: 지난달 파일을 고치자 mtime이 최신이 되어 `findLatestExcel`이
  **26/7을 당월로 집었다.** 인자 없이 `npm run deploy`를 돌렸으면 7월 데이터가 당월로 발행될 뻔했다.
  → 파일명 기준월이 가장 늦은 것을 고르고 같은 달 안에서만 mtime으로 고르도록 수정(`tools/lib/publish.mjs`).
- 검증: 라이브 되읽기 전월 `7/8/26 62.23억` → 당월 `78.66억` · 생산처 향남 65.23 / 횡성 13.43 ·
  PSI 134행·기준일 8/1/26 무회귀

### 담당자 확인 요청 중 1건 해결됨

아래 "담당자 확인 요청"의 3번(26/7 채널 미부착)은 이걸로 끝났다. 1·2·4번은 그대로 남아 있다.

## 남은 제안 — 사용자 결정 대기

1. **품목군 전월대비에 생산처 축 추가** — "횡성 때문에 얼마 늘었나"가 화면에서 바로 나온다.
   전월 스냅샷(`pkg_prev`) 구조 변경 필요 → `tools` + `SOP_LATEST.html` 양쪽
2. **전월 누락(채널 미부착) 경고** — 1번을 하면 반드시 같이 해야 한다.
   안 하면 "횡성 0 → 13.43억, 신규"라는 오답이 화면에 뜬다(실제 +7.05억)

## 담당자 확인 요청 (엑셀 원장)

~~1. 보류재고 금액(O열)이 전 행 비어 있다~~ → ✅ **0이 맞다** (2026-08-18 사용자 확정).
   보류 수량 1,183,498개는 그대로 Aging 구간에 집계되고 금액만 0이다. 정상 동작.
~~2. 전 행 금액이 0인 자재 3건, 약 2.09억~~ → ✅ **0으로 둔다** (2026-08-18 사용자 확정).
   `9401386 멀티비타민 미네랄`(85,320개) / `7301428 리유저블백` / `7301427 돗자리`.
   결산 자재수불 단가로 메우면 총액이 2.09억 커지지만 그건 Aging 기준이 아니다.
~~3. 26/7 파일의 채널 미부착 9.83억~~ → ✅ **담당자가 수식을 414행까지 채워 해결** (위 ㉒ 참고).

⚠ **1·2번은 "고쳐야 할 것"이 아니라 확정된 업무 기준이다.** 총액이 2.09억 작아 보인다고
다음 세션에서 단가로 메우려 들지 말 것 — `CLAUDE.md`의 `parseInventory` 항목과 코드 주석에 못박아뒀다.

4. ⚠ **SKU별 금액은 신뢰도가 낮다 (남아 있음)** — 기초재고 시트에서 한 자재의 금액이 일부 배치에만 몰려 있다
   (503행 중 208행·수량 35%가 금액 0). 자재 단위로는 결산 대비 ±1.7억까지 흔들린다.
   **총액·구간별 합계는 −0.52억 차이로 신뢰 가능**하지만 드릴다운의 SKU 금액은 참고용으로만 볼 것.

## 합의된 코드 작업 대기열

### 1. 오차 금액 차트 라벨 개선 — **사용자가 "좀 더 고민해보자"로 보류 (2026-08-16)**

사용자가 `오차 금액 · 계획 대비` 차트 해석이 어렵다고 함. 뜻은 설명해서 이해됨.
**아래 변경안을 제시했고 답을 기다리는 중.** 사용자가 다시 꺼내기 전엔 착수하지 말 것.

| | 현재 | 제시한 변경안 |
|---|---|---|
| 제목 | 오차 금액 · 계획 대비 | 계획보다 덜 팔림 / 더 팔림 (금액) |
| 범례 | 미달(계획 못 채움) / 초과(계획 넘김) | ← 덜 팔림 / 더 팔림 → |
| 축 | ← 미달  초과 → (억) | 덜 팔린 금액 ← 0 → 더 팔린 금액 (억원) |
| 설명 | 막대 길이 = 오차 금액… | 막대가 길수록 계획이 많이 틀린 것 · 양쪽으로 다 길면 총액은 맞았지만 품목 배분이 틀린 것 |

차트가 말하려는 것(설명하면 사용자가 납득한 내용):
- 막대 전체 길이 = 그 채널이 만든 오차 총량 / 치우친 쪽 = 덜 팔렸나 더 팔렸나
- 오른쪽 숫자 = 순차이(초과 − 미달)
- **직영몰 패턴이 핵심**: 순차이 −0.89억이라 잘 맞춘 것처럼 보이지만 미달 5.57억과 초과 4.68억이
  상쇄된 것. 총액은 맞고 **품목 배분이 틀린** 상태 → 순차이 하나만 보면 이걸 놓친다

### 2. (미착수, 사용자 제안 대기) 표 행 줄이기

채널 11개 중 계획 0.5억 미만 5개(제로샵·올리브영·복지몰·벤더·편의점)가 합쳐 1.3억인데 행의 절반을 차지.
접으면 6줄. 최근 6개월만 표시하는 안도 같이 제안했으나 둘 다 사용자 응답 없음.

## 사용자가 해야 진행되는 것 (코드 작업 아님)

| 할 일 | 안 하면 |
|---|---|
| PSI 시트 **11~12월 입고(공급계획)** 채우기 | v1.4에서 10월은 28건으로 늘었지만 **11·12월은 0건**. 11월 음수 기말 10건 |
| 오차율분석 시트 **4월 2303xxx 실적을 7302153으로** 옮기기 | 4월 WAPE가 118.4%로 튄다. 합치면 87.0% (아래 미해결 #1) |
| 단가 정정표에 **9401385**(밀크씨슬 횡성) 추가 | DAI-17 원가가 SAP값 3,208원으로 잡혀 매출 2,270원을 넘는다. PSI 원가 약 2.8억 과대 |

**v1.4(8/14)에서 해소된 것** — 26년 사업계획 목표 밀림 ✅ / PSI 12월 판매계획 ✅ (11월 신호등 부활)
**v1.1(8/13)에서 해소된 것** — 오차율분석 7월 판매계획(228건 76.8억) ✅ / [수량] 판매계획 AU~BF ✅
(`hit.qty.hasPlan=true`) / 평균출하(3평판) 갱신 ✅ / SF 26년 7월 실적 73.4억 ✅

## 미해결 이슈

### 1. ③탭 4월 오차율 118.4% — 실적이 다른 자재코드로 붙었다 (2026-08-13 확인)

```
7302153 (합쎈 6 IN 1 60캡슐)          2303xxx 코드군
  3월 계획 14.24  실적 12.18            1~3월 실적 0
  4월 계획 14.17  실적  1.12   ←        4월 실적 14.65  ←  이 달만
  5월 계획 13.59  실적  9.81            5~7월 실적 0
```

2303624·2303626은 **PSI에도 기준정보에도 없는 코드**다. WAPE 분자에 두 번(계획측 미달 + 실적측 초과)
잡혀서, 4월 |차이| 98.38억 중 **27.67억(28%)이 이 한 쌍**에서 나온다. 합치면 118.4% → **87.0%**.
곁가지로 계획 100만원 미만 행이 4월에 78건 있는데, 이걸 빼면 4월만 95.9%로 움직이고 다른 달은 0.2~0.4%p뿐 —
**산식 문제가 아니라 4월 고유의 데이터 문제**다. 엑셀에서 고치는 게 맞다.

### 1-2. 4월 카카오 오차 836% — 직영몰 상품이 카카오 채널로 붙었다 (2026-08-16 확인)

새로 만든 채널 오차 팝업(`openChDrill`)에서 드러났다. 카카오 4월 계획 0.91억인데 실적 6.80억:

| 품목 | 계획 | 실적 | 기여도 |
|---|---|---|---|
| **직영몰** 비타민B_30정_코스맥스바이오 | 0.02 | **4.34** | 56.7% |
| [포장재] 에너씨슬 플래티넘 골프공 세트 | 0.00 | 1.87 | 24.5% |
| **직영몰**_루테인아스타잔틴_30캡슐 | 0.00 | 0.19 | 2.4% |

품목명이 `직영몰`로 시작하는데 채널은 카카오다. **오차 예측 실패가 아니라 채널 분류 오류**로 보인다.
`[포장재]` 항목들도 계획 없이 실적만 잡혀 오차율이 수십만 %로 나온다(판매계획 대상이 아닌 듯).
위 #1(2303xxx)과 함께 4월 전체 WAPE 118.4%를 만드는 주범. **엑셀에서 고칠 일.**
`[포장재]`를 오차 분석에서 빼는 옵션은 제안만 하고 미착수.

### 1-3. SF 시트 브랜드 섹션 계획 합계가 총계와 안 맞음 (2026-08-16 확인)

8~12월 **계획** 구간에서 브랜드 섹션 합(8월 75.69억)이 채널 섹션·`총계` 행(78.89억)과 3~5억 벌어진다.
실적 구간(1~7월)은 정확히 일치. **대시보드가 아니라 원본 시트 문제**라 코드로는 못 고친다.
화면에서는 브랜드별/채널별 토글에 따라 합계가 다르게 보인다.

### 1-4. 사업계획 목표는 순매출 기준인데 '매출' 모드에서도 그 목표로 나눈다

시트 `GAP` 행 역산으로 확인: `GAP = 순매출(신제품제외) − 26년 사업계획` (1월 54.00−50.50=+3.50 ✅).
즉 매출(총액) 모드의 달성율은 분자·분모 기준이 어긋나 부풀려진다(1월 119.8% vs 순매출 기준 106.9%).
**사용자가 "매출기준 순매출기준 둘 다 토글로 본다"고 판단해 그대로 두기로 함 (2026-08-16).**
표 양식도 건드리지 않기로 함 — 달성율 행을 늘리는 안은 기각.

### 2. 당월실적 시트가 0품목 — **원인 확인됨 (2026-08-13), 수정 대기**

`actual_unit`이 계속 0건. 원인은 **시트 범위가 `B1`부터 시작인데 `parseActualQtySheet`가 A열 기준으로
인덱스를 잡은 것** — PSI 시트와 똑같은 오프셋 함정이다.

```
코드 주석: X=23 자재, AZ=50 매출금액, BB=52 매출수량
실제 값  : idx23="제품군", idx50="매출수량", idx52="반품금액"(=0)
```

`qty<=0`에 전건이 걸러진다. 헤더 행에서 열 이름으로 인덱스를 찾게 고치면 된다.
영향: PSI 시뮬레이션 단가 폴백(`실적단가`), ③탭 오차 인사이트 수량 추정(`qtyFromAmountM`).

### 2-1. 엑셀 시트 정리 (2026-08-13 조사)

`실적수량` 시트는 **코드도 안 읽고 수식 참조도 0건** → 지워도 안전.
반면 `평균출하`는 코드는 안 읽지만 **PSI!F열(3평판)이 683건 수식으로 참조** → 지우면 3평판이 `#REF!`,
`원가`는 코드가 직접 읽음(매출환산 열) → 둘 다 삭제 금지.

### 3. 소진계획 메모에 월 개념이 없음

`data-plan-key`가 `AGING|채널|구간|SKU` / `PSI|자재코드` 형식이라 월 구분이 없다.
지난달 메모가 이번 달 행에 그대로 남는다. 숫자에는 영향 없음.

## 세션 이어가기

```bash
claude --continue     # 이 폴더의 가장 최근 대화 이어서
claude --resume       # 대화 목록에서 골라서
```

대화 기록은 `~/.claude/projects/-workspaces-s-op-gungi/*.jsonl`에 있다.
**Codespace 안에만 있으므로 Codespace를 삭제하면 사라진다** (GitHub 기본 30일 미사용 시 자동 삭제).
대화가 사라져도 `CLAUDE.md` + `WORK_LOG.md` + 이 파일 + git 커밋 메시지면 맥락은 복원된다.

## 작업 원칙 (사용자가 명시한 것)

- **코드 수정 전에 방향을 물어볼 것.** 조사·진단은 묻지 않고 진행해도 됨
- PSI 관련은 최소 변경
- 루트 `SOP_LATEST.html`을 고치면 **반드시** `cp SOP_LATEST.html public/SOP_LATEST.html`
- 검증은 Node vm 하네스로 (scratchpad의 `qtytest.mjs`, `psitest.mjs`, `arctest.mjs` 패턴)
