{
  "name": "s-op-gungi",
  "private": true,
  "scripts": {
    "deploy": "node tools/deploy.mjs",
    "deploy:check-auth": "node tools/deploy.mjs --check-auth",
    "publish:data": "node tools/publish-data.mjs",
    "release:live": "node tools/deploy.mjs --code-only"
  }
}
